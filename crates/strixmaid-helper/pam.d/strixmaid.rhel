#%PAM-1.0
# StrixMaid 的 PAM 服务配置 —— RHEL / Fedora / Rocky / Alma 系模板。
# 安装到 /etc/pam.d/strixmaid（config.toml 的 pam_service 默认即 "strixmaid"）。
#
# 认证 / 账户 / 密码栈直接复用 system-auth（authselect 管理），
# 因此 pam_unix、SSSD、2FA 等只要在 system-auth 里生效，这里就自动生效。
auth       required     pam_env.so
auth       substack     system-auth
auth       include      postlogin
account    required     pam_nologin.so
account    include      system-auth
password   include      system-auth

# 会话：pam_systemd 负责创建 loginctl 会话与 XDG_RUNTIME_DIR（用户级 unit 的前提，design.md §5.4）。
session    required     pam_loginuid.so
session    optional     pam_keyinit.so force revoke
session    include      system-auth
session    include      postlogin
