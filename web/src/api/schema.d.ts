/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/api/v1/audit": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * 查询审计日志
         * @description **需要管理访问**：会话未提权时返回 403 `elevation_required`，前端应引导用户提权后重试
         *     （错误体的 `retry_elevated` 为 `true`）。理由见模块文档——审计表在主进程库里，
         *     没有可外包给操作系统的权限。
         *
         *     结果按 `id` 降序（新的在前）。翻页把上一页的 `next_before_id` 原样传回 `before_id`，
         *     并保持其余过滤条件不变；`next_before_id` 为空表示已到最旧一条。
         *     `limit` 缺省 100、上限 1000，`0` 或越界都是 400。
         *     时间范围是左闭右开的 `[since, until)`。
         */
        get: operations["list_audit"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/auth/elevate/respond": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * 回应提权提示
         * @description 与 `/auth/respond` 同形。成功时 `token` 回传当前 token（不轮换）。
         */
        post: operations["elevate_respond"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/auth/elevate/start": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * 开始提权
         * @description 对当前会话再走一次 PAM 对话（sudo 语义：用自己的密码；传别的用户名则是 su 语义）。
         *     成功后会话获得 admin worker，`elevated = true`。
         */
        post: operations["elevate_start"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/auth/logout": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * 登出
         * @description 终止本会话的 worker、关闭 PAM 会话、删除会话记录。幂等：会话已不存在时同样 204。
         */
        post: operations["logout"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/auth/respond": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * 回应登录提示
         * @description 返回 `status = "complete"`（含 Bearer token）或 `status = "more"`（继续追问）。
         *     认证失败是 401。
         */
        post: operations["respond"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/auth/session": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * 当前会话
         * @description 本会话在当前节点上的认证状态；每次调用都会刷新活跃时间。
         */
        get: operations["session"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/auth/start": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * 开始登录
         * @description 拉起一个 PAM 对话，返回第一轮提示。`session` 是短生命周期的认证会话 id（60 秒），
         *     不是登录 token。
         */
        post: operations["start"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/capabilities": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * 能力探测
         * @description `system`：这台机器有没有（启动时探测一次）；`user`：当前登录用户能不能用
         *     （按组推导 + user worker 内实测，实测结果按会话缓存 60 秒）。
         *     **未认证时 `user` 为 `null`，仍返回 200。**
         */
        get: operations["capabilities"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/files": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * 列目录
         * @description 在会话用户的 worker 内执行，可见性由文件权限裁决。目录在前、其余按名称排序；
         *     `lstat` 失败的条目被跳过并计入 `skipped`。
         */
        get: operations["list_dir"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/files/content": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * 查看文本文件
         * @description 只支持文本：二进制（前 8 KiB 含 NUL）与超过 5 MiB 的文件返回 400。
         *     无效 UTF-8 序列被替换成 U+FFFD 并以 `lossy = true` 标出。
         */
        get: operations["read_file"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/health": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * 健康检查
         * @description 无需认证。用于反向代理探活、部署校验，以及前端确认后端版本。
         */
        get: operations["health"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/logs": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * 查询日志
         * @description 由新到旧一页；翻页带上 `cursor`（上一页的 `next_cursor`）与**相同的过滤条件**。
         *     `limit` 缺省 100、上限 1000。`q` 是字面量关键字（不是正则），大小写不敏感。
         *     结果集只含登录用户可见的条目（见模块文档）。
         *
         *     **提权会扩大可见范围**：先以登录用户的身份读，被 journald 拒绝时，若会话已提权
         *     就换管理身份重试（`auth::exec::escalate`）——与用户在终端里 `sudo journalctl`
         *     得到的结果一致。未提权时返回 403 并带 `can_retry_elevated`，前端据此提示提权。
         *     升级成功的那次会留审计（以管理身份读系统日志不是无痕操作）；未升级的读不审计。
         */
        get: operations["query_logs"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/logs/boots": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * boot 列表
         * @description 按 `index` 升序，`0` 为本次启动。`boot_id` 可直接作为查询参数 `boot` 的值。
         *     同样受 ACL 影响：读不到系统日志的用户只会看到自己有条目的那些 boot。
         */
        get: operations["list_boots"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/logs/entry/{cursor}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * 单条日志全字段
         * @description 游标需 URL 编码。已被轮转淘汰的条目返回 404；**对登录用户不可见的条目同样是 404**——
         *     journalctl 的行为就是「查不到」，服务端不去区分「不存在」与「无权看见」，
         *     区分本身就会泄露日志的存在。
         */
        get: operations["log_entry"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/logs/usage": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * 日志磁盘占用与清理能力
         * @description `bytes` 为 `null` 表示测不到（如 macOS 的日志库目录对非 root 不可读）。
         *     `modes` 是本机支持的清理方式，清理对话框按它渲染：journald 支持按保留期 /
         *     目标大小收缩，macOS 统一日志只有「全部抹除」。
         */
        get: operations["log_usage"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/logs/vacuum": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * 清理日志
         * @description `keep_secs`（只留最近一段）/ `max_bytes`（收缩到目标大小）/ `erase_all`（全部抹除，
         *     仅 macOS）**三选一**。journald 只清已归档文件，当前活跃文件不动，清理后占用不会
         *     精确等于期望值。写操作：入审计；权限由底层裁决（journald 文件属主 / macOS root），
         *     未提权被拒时返回 403 + `can_retry_elevated`，提权后自动以管理身份重试。
         */
        post: operations["log_vacuum"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/metrics/current": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * 实时快照
         * @description 最近一轮采集的全部瞬时值，与 WS `metrics.live` 频道推送的 payload 相同。
         *     进程刚启动、第一轮尚未完成时 `values` 为空。`?node=` 指向 Agent 节点时
         *     返回该节点最近一帧 `agent.snapshot`。
         */
        get: operations["current"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/metrics/query": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * 查询时序（自动选层）
         * @description 跨度落在内存环覆盖范围内且 `step < 60` 时直接从环出（`layer = live`，`step` 取整到
         *     采集间隔的倍数）；否则按 design.md §7.2 的三条规则在五个落盘层里选最粗的合适层。
         *     区间左闭右开 `[from, to)`；请求里存在但查无此序列的项不会出现在结果里。
         */
        get: operations["query"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/metrics/series": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * 可用 series 列表
         * @description 以 `series` 表为准：包含历史上出现过、现在已不再采集的序列（例如已拔掉的磁盘）。
         *     未配置落盘时来自内存环，此时 `id` 为 0。
         */
        get: operations["series"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/nodes": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * 节点列表
         * @description 含本机（`local`）与已登记的 Agent。`online` 来自内存注册表（此刻是否有
         *     存活连接），`last_seen` 优先取内存里更新鲜的值。
         */
        get: operations["list_nodes"];
        put?: never;
        /**
         * 登记节点
         * @description 生成预共享 token（**仅在本响应里出现一次**，服务端只存 hash）。
         *     Agent 配置的 `node_id` / `token` 从响应里抄。需要管理访问。
         */
        post: operations["create_node"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/nodes/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * 删除节点
         * @description Agent 的当前连接会在下一帧因 token 失效……并不会：连接建立时已完成鉴权，
         *     删除只保证**新的**连接被拒。该节点已汇聚的 series 与桶数据保留（历史仍可查），
         *     由保留期清理自然过期。需要管理访问。
         */
        delete: operations["delete_node"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/processes": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * 进程列表
         * @description 平铺数组，树由前端按 `ppid` 拼。`tree=true` 时命中项的全部祖先一并返回并按深度优先排序。
         *     CPU% 为两次请求之间的差分；差分基线在本会话的 worker 内，因此**每个会话的首次请求为 0**。
         */
        get: operations["list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/processes/{pid}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * 进程详情
         * @description cmdline / cwd / exe / 环境变量 / fd / IO / cgroup 与所属 systemd unit。
         *     `cwd` / `exe` / `environ` / `fds` 只有同 uid 或 root 能读，否则为 `null`——
         *     判断的依据是**登录用户**，因为读取动作发生在该用户的 worker 里。
         */
        get: operations["detail"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/processes/{pid}/renice": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * 调整 nice 值
         * @description `setpriority(2)`。调低（提高优先级）需要 root，非特权用户只能调高自己的进程。
         *
         *     与信号同一套规则：先以登录用户的身份试，被内核拒绝且已提权时改用管理身份重试；
         *     未提权则返回 403 并带 `can_retry_elevated = true`。「把自己的进程调低优先级」
         *     是无需提权的日常操作，不该被一刀切成管理操作。
         */
        post: operations["renice"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/processes/{pid}/signal": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * 发送信号
         * @description 只开放 `term` / `kill` / `hup`。**权限完全由内核裁决**，服务端不做任何判断。
         *
         *     先以登录用户的身份发（自己的进程本就该发得动）；内核回 `EPERM` 且会话已提权时
         *     自动改用管理身份重试一次。未提权时返回内核给出的原始 403，并带
         *     `can_retry_elevated = true` —— 前端据此提供「启用管理访问后重试」，
         *     而不是把它显示成一条死路。
         */
        post: operations["signal"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/services": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * unit 列表
         * @description 已加载的 unit 与仅存在于磁盘的 unit 文件合并后按名字排序；可按类型 / 活动状态 /
         *     是否开机自启 / 关键字过滤。`scope=user` 需要 user manager 可达。
         */
        get: operations["list_units"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/services/timers": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * 定时任务列表
         * @description systemd `*.timer`（Linux）与 launchd 定时 job（macOS）统一成一张表。字段缺失都有含义：
         *     `schedule` 为空数组 = 拿不到调度规则（systemctl 降级路径）；`next_ts` 为 `null` =
         *     推算不了（timer 已停、launchd `StartInterval` 型）；`last_ts` 为 `null` = 来源不记录。
         *     cron 是规划中的第三来源，实现前不出现。
         */
        get: operations["list_timers"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/services/{unit}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * unit 详情
         * @description 含主进程 pid、时间戳、重启次数、上次结果，以及直读 `/sys/fs/cgroup` 的 CPU / 内存 / 任务数。
         *     `cgroup.cpu_percent` 需要两次采样：首次请求为 `null`，之后每次相对上一次计算。
         *     差分基线是 worker 内的实例状态，因此「首次」是**每个会话各一次**。
         */
        get: operations["unit_detail"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/services/{unit}/action": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * 对 unit 执行操作
         * @description `start` / `stop` / `restart` / `reload` 是异步的：返回只表示 job 已入队，
         *     终态请订阅 WS `services.changed`。`enable` / `disable` / `mask` / `unmask` 会改写符号链接
         *     并触发 `daemon-reload`。
         *
         *     能不能做由 polkit 对**登录用户**裁决，服务端不预判。
         */
        post: operations["unit_action"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/services/{unit}/deps": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * unit 依赖关系
         * @description `Requires` / `Wants` / `After` / `Before` / `TriggeredBy` 等属性原样返回，供前端画依赖图。
         */
        get: operations["unit_deps"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/services/{unit}/file": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * unit 文件原文
         * @description 主文件（`FragmentPath`）与 drop-in 覆盖文件（`DropInPaths`）的原文，不做任何解析。
         *     transient unit 没有主文件，`fragment` 为 `null`。
         *     读文件的是 worker，因此可读性由文件权限对登录用户裁决。
         */
        get: operations["unit_file"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/system/health": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * 健康聚合
         * @description 需重启 / 文件系统容量与 inode 超阈值 / 负载过高 / 根文件系统只读。
         *     failed units 与 SMART 由其它模块补充，`skipped` 里如实标出「未检查」。
         *
         *     在 user worker 内执行，因此「读不到」与「无权读」的结果对登录用户是真实的。
         */
        get: operations["system_health"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/system/hostname": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * 修改主机名
         * @description `sethostname(2)` 立即生效并写入 `/etc/hostname`；`pretty_hostname` 写入 `/etc/machine-info`。
         *
         *     **需要管理访问**：改的是整机标识，在 admin worker（uid = 0）内执行。
         *     会话未提权时不做任何事，直接 403 `elevation_required`。
         */
        put: operations["set_hostname"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/system/info": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * 主机信息
         * @description 主机名 / 发行版 / 内核 / 架构 / 虚拟化 / DMI / CPU / 内存 / 块设备 / 文件系统 / 开机时长。
         *     全部直读 `/proc`、`/sys`、`/etc`，读不到的字段为 `null`。
         */
        get: operations["info"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/system/power": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * 重启 / 关机
         * @description 调 `systemctl reboot|poweroff`。命令被受理即返回 202，真正的关机是异步的；关机等于远程失联，前端必须二次确认。
         *
         *     **需要管理访问**：在 admin worker（uid = 0）内执行，未提权返回 403 `elevation_required`。
         */
        post: operations["power"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/system/time": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * 时间与时区
         * @description 服务端时刻、IANA 时区、UTC 偏移、NTP 服务与同步状态（`adjtimex`）、RTC 模式。不调 `timedatectl`。
         */
        get: operations["time"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/system/timezone": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * 修改时区
         * @description 原子替换 `/etc/localtime` 软链（并更新 `/etc/timezone`，如存在）。时区名必须存在于 `/usr/share/zoneinfo`。
         *
         *     **需要管理访问**：在 admin worker（uid = 0）内执行，未提权返回 403 `elevation_required`。
         */
        put: operations["set_timezone"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/terminals": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * 终端列表
         * @description 只返回**本会话**的终端。别的会话的终端既看不到也用不了。
         */
        get: operations["list_terminals"];
        put?: never;
        /**
         * 新建终端
         * @description 在会话用户的 worker 里开一个 PTY 并启动 shell。返回的 `id` 用于拼
         *     `WS /ws/terminal/{id}`，**只在本会话内有效**。
         */
        post: operations["create_terminal"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/terminals/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * 关闭终端
         * @description 向 worker 发 `term.close`，`SIGHUP` 进程组并回收 PTY。附着中的 WS 会随之关闭。
         */
        delete: operations["delete_terminal"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/terminals/{id}/resize": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * 改终端尺寸
         * @description 与 WS 里的 `{"t":"resize"}` 完全等价，供没有附着 WS 时使用
         *     （`roadmap/03-terminal.md` §4.4）。**不写审计**：改窗口大小不是对系统做了什么，
         *     把它记下来只会把真正要看的记录淹掉。
         */
        post: operations["resize_terminal"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /** @description 统一错误响应体。所有非 2xx 的 REST 响应、以及 WS `t = "err"` 消息的 `d` 字段，都是这个形状。 */
        ApiError: {
            /**
             * @description 提示前端「提权后重试即可成功」。通常与 [`ErrorCode::ElevationRequired`] 或
             *     [`ErrorCode::PermissionDenied`] 同时出现；为 `false` 表示提权也无济于事。
             */
            can_retry_elevated?: boolean;
            /**
             * @description 仅当 `code` 为 [`ErrorCode::CapabilityUnavailable`] 时有值：缺失的能力名，
             *     取值与 [`crate::capability::SystemCapabilities`] 的字段名一致（如 `"systemd"`、`"journal"`）。
             *     前端据此隐藏对应页面。
             * @example systemd
             */
            capability?: string | null;
            /** @description 机器可读的错误分类。 */
            code: components["schemas"]["ErrorCode"];
            /**
             * @description 补充细节：底层错误原文、polkit action id、`systemctl` 的 stderr 等。
             *     为 `None` 表示没有更多信息。
             */
            detail?: string | null;
            /**
             * @description 面向用户的一句话说明，可直接展示。**不得包含明文凭据**（见 `docs/design.md` §5.3）。
             * @example 当前账户不在 systemd-journal 组，只能看到自己的日志
             */
            message: string;
        };
        /** @description 一条审计记录，与 `audit_log` 表一一对应。 */
        AuditEntry: {
            /**
             * @description 操作标识，点分层级，与 REST 端点对应：`service.start` / `process.kill` /
             *     `file.write` / `auth.login` / `auth.elevate` / `system.power`。
             * @example service.start
             */
            action: string;
            /** @description 结果说明：失败原因、polkit action id 等。成功时通常为 `None`。 */
            detail?: string | null;
            /** @description 操作时会话是否处于提权状态。 */
            elevated: boolean;
            /**
             * Format: int64
             * @description 自增主键，同时用作翻页游标（见 [`AuditQuery::before_id`]）。
             * @example 10234
             */
            id: number;
            /**
             * @description 目标节点 id。MVP 恒为 `"local"`。
             * @example local
             */
            node_id: string;
            /**
             * @description 补充参数，任意 JSON 对象。
             *
             *     **禁止写入明文密码、token 或其它凭据。**
             */
            params?: Record<string, never>;
            /** @description 请求来源地址。经反向代理时可能是代理地址或为 `None`。 */
            remote_addr?: string | null;
            /** @description 结果。 */
            result: components["schemas"]["AuditResult"];
            /**
             * @description 操作对象：unit 名 / pid / 文件路径。无明确对象（如 `auth.login`）时为 `None`。
             * @example nginx.service
             */
            target?: string | null;
            /**
             * Format: int64
             * @description 操作发生的时刻。
             * @example 1756252800
             */
            ts: number;
            /**
             * Format: int32
             * @description 发起者的 uid。历史记录里可能缺失，为 `None`。
             * @example 1000
             */
            uid?: number | null;
            /**
             * @description 发起者的系统用户名。
             * @example alice
             */
            username: string;
        };
        /** @description `GET /api/v1/audit` 的响应体。 */
        AuditPage: {
            /** @description 本页记录，按 `id` **降序**（新的在前）。 */
            entries?: components["schemas"]["AuditEntry"][];
            /**
             * Format: int64
             * @description 下一页的 [`AuditQuery::before_id`]。为 `None` 表示已到最旧一条。
             */
            next_before_id?: number | null;
        };
        /**
         * @description 操作结果。
         * @enum {string}
         */
        AuditResult: "ok" | "denied" | "error";
        /**
         * @description `POST /api/v1/auth/respond` 与 `POST /api/v1/auth/elevate/respond` 的**成功**响应体。
         *
         *     用内部判别字段 `status`（而不是 untagged）：
         *
         *     ```jsonc
         *     { "status": "complete", "token": "...", "user": { ... } }
         *     { "status": "more",     "session": "...", "prompts": [ ... ] }
         *     ```
         *
         *     理由有两条，都指向 OpenAPI 这个 P0 硬要求：
         *
         *     1. untagged 生成的 `oneOf` 里各分支没有任何可判别的标记，代码生成器只能猜；
         *     2. untagged 靠「字段集互不相交」区分分支，将来任一分支加字段都可能**静默**匹配到错分支。
         *
         *     生成的 schema 里每个分支都把 `status` 声明为**必填的单值枚举**
         *     （`{"status": {"type": "string", "enum": ["complete"]}}`），这是 JSON Schema 原生的
         *     判别方式，各主流生成器都认。这里没有用 OpenAPI 的 `discriminator` 关键字：
         *     utoipa 5.5 只在 `#[serde(untagged)]` + 单字段 `$ref` 变体上才生成它，
         *     而那恰好又回到了上面两个问题里。单值枚举比 `discriminator` 更严格——
         *     校验器会真的拒绝 `status` 与内容不匹配的响应，`discriminator` 只是个提示。
         *
         *     **失败不在这个 union 里**：认证失败是 HTTP 401 + [`crate::ApiError`]（`code` 为
         *     [`crate::ErrorCode::Unauthenticated`]），走与其它错误完全相同的通道。
         */
        AuthOutcome: {
            /** @enum {string} */
            status: "complete";
            /**
             * @description 登录 token（Bearer）。
             *
             *     # ⚠️ 敏感
             *
             *     服务端**只存它的 hash**（`sessions.id`），绝不落明文；同样禁止进日志。
             *     提权成功时该字段回传原 token 或轮换后的新 token，由服务端决定。
             * @example smt_9d1f...
             */
            token: string;
            /** @description 认证到的系统身份。 */
            user: components["schemas"]["AuthUser"];
        } | {
            /** @description 新一轮提示，`id` 重新编号。 */
            prompts: components["schemas"]["Prompt"][];
            /** @description 继续使用的认证会话 id（可能与上一轮相同）。 */
            session: string;
            /** @enum {string} */
            status: "more";
        };
        /**
         * @description `POST /api/v1/auth/respond` 与 `POST /api/v1/auth/elevate/respond` 的请求体。
         *
         *     # 安全
         *
         *     内含明文凭据，**不实现 `Serialize` / `Clone`**。`Debug` 因 [`PromptResponse`] 已脱敏而安全，
         *     但仍不建议整体打日志。`responses` 里每一项的 `value` 都会在 drop 时自动擦除。
         *     见模块文档。
         */
        AuthRespondReq: {
            /** @description 对本轮 `prompts` 中所有 [`PromptStyle::needs_input`] 项的回应，顺序无关。 */
            responses?: components["schemas"]["PromptResponse"][];
            /**
             * @description [`AuthStartResp::session`] 或上一轮 [`AuthOutcome::MorePrompts`] 给出的会话 id。
             * @example 0f3c1a9e7b2d4f60
             */
            session: string;
        };
        /** @description `POST /api/v1/auth/start` 与 `POST /api/v1/auth/elevate/start` 的请求体。 */
        AuthStartReq: {
            /**
             * @description 要认证的系统用户名。PAM 需要在会话开始时就知道它。
             *
             *     提权（elevate）时通常传当前会话的用户名（`sudo` 语义：用自己的密码提权）；
             *     若要以别的账户提权（`su` 语义），传目标用户名。
             * @example alice
             */
            username: string;
        };
        /** @description `POST /api/v1/auth/start` 的响应体。 */
        AuthStartResp: {
            /** @description 本轮需要用户处理的提示，可能为空数组（PAM 模块无需交互时）。 */
            prompts?: components["schemas"]["Prompt"][];
            /**
             * @description 短生命周期的**认证会话** id，用于把多轮 respond 串起来。
             *
             *     注意：这不是登录 token——它只代表「一次进行中的 PAM 对话」，认证成功后即作废。
             *     服务端应给它一个短超时（分钟级），超时后返回 [`crate::ErrorCode::NotFound`]。
             * @example 0f3c1a9e7b2d4f60
             */
            session: string;
        };
        /**
         * @description 认证成功后返回的用户身份。
         *
         *     字段与 helper 的 `AuthOk { uid, gid, username, groups }`（`docs/design.md` §10）一致。
         */
        AuthUser: {
            /**
             * Format: int32
             * @description 主组 gid。
             * @example 1000
             */
            gid: number;
            /** @description 所属组名列表（含主组）。用于前端判断「不在 `systemd-journal` / `adm` 组」这类提示。 */
            groups?: string[];
            /**
             * Format: int32
             * @description 系统 uid。
             * @example 1000
             */
            uid: number;
            /**
             * @description 系统用户名。
             * @example alice
             */
            username: string;
        };
        /** @description `GET /api/v1/logs/boots` 的列表项（对应 `journalctl --list-boots`）。 */
        BootInfo: {
            /**
             * @description 32 位 hex 的 boot id。跨重启稳定，比 `index` 可靠（`index` 会随新启动而整体平移）。
             * @example 4d3a2b1c5e6f70819a2b3c4d5e6f7081
             */
            boot_id: string;
            /**
             * Format: int64
             * @description 该 boot 内第一条日志的时刻。
             */
            first_ts: number;
            /**
             * Format: int32
             * @description 相对序号：`0` = 本次启动，`-1` = 上一次，依此类推。可直接作为 [`LogQuery::boot`] 的值。
             * @example 0
             */
            index: number;
            /**
             * Format: int64
             * @description 该 boot 内最后一条日志的时刻。对当前 boot 即「刚刚」。
             */
            last_ts: number;
        };
        /** @description `GET /api/v1/capabilities` 的响应体。 */
        Capabilities: {
            /**
             * @description 登录前可见的最小主机身份。
             *
             *     登录页要展示「你在登录哪台机器」（主机名、发行版、内核）并按发行版取主题色，
             *     而 `/system/*` 全在认证墙后。这里只暴露与 SSH banner / Cockpit 登录页同级的
             *     信息量，不含硬件、网络、磁盘等细节。
             */
            identity?: components["schemas"]["HostIdentity"];
            /** @description 机器级能力，启动时探测一次，与登录者无关。 */
            system: components["schemas"]["SystemCapabilities"];
            user?: null | components["schemas"]["UserCapabilities"];
        };
        /**
         * @description unit 的 cgroup 资源占用（`/sys/fs/cgroup/<ControlGroup>/`）。
         *
         *     每个字段都可能为 `None`：cgroup v1/v2 暴露的文件不同，容器内还可能整棵树都不可读。
         */
        CgroupUsage: {
            /**
             * Format: double
             * @description 最近一个采样窗口内的 CPU 占用，**百分比**。单核跑满为 100.0，
             *     多核可超过 100.0（4 核跑满 = 400.0）。服务端未做两次采样时为 `None`。
             * @example 3.5
             */
            cpu_percent?: number | null;
            /**
             * Format: int64
             * @description 累计 CPU 时间，**纳秒**（`cpu.stat` 的 `usage_usec` × 1000 或 systemd `CPUUsageNSec`）。
             *     这是单调累加值，算占用率要自己做两次采样求差。
             * @example 12345678900
             */
            cpu_usage_nsec?: number | null;
            /**
             * Format: int64
             * @description 当前内存占用，字节（`memory.current` / systemd `MemoryCurrent`）。
             *     注意它**含 page cache**，通常比 RSS 大。
             * @example 268435456
             */
            memory_current_bytes?: number | null;
            /**
             * Format: int64
             * @description 内存上限，字节（`memory.max` / `MemoryMax`）。未设限（`max`）时为 `None`。
             */
            memory_limit_bytes?: number | null;
            /**
             * Format: int64
             * @description 历史峰值内存，字节（`memory.peak`）。cgroup v1 或旧内核为 `None`。
             */
            memory_peak_bytes?: number | null;
            /**
             * @description cgroup 路径（systemd `ControlGroup`），如 `/system.slice/nginx.service`。
             * @example /system.slice/nginx.service
             */
            path?: string | null;
            /**
             * Format: int64
             * @description 当前任务（线程）数（`pids.current` / `TasksCurrent`）。
             */
            tasks_current?: number | null;
            /**
             * Format: int64
             * @description 任务数上限（`TasksMax`）。未设限为 `None`。
             */
            tasks_limit?: number | null;
        };
        /** @description CPU 信息（`/proc/cpuinfo` + `/sys/devices/system/cpu`）。 */
        CpuInfo: {
            /**
             * Format: int32
             * @description **逻辑**核数（含超线程），即 `/proc/stat` 里 `cpuN` 的条数。
             * @example 16
             */
            logical_cores: number;
            /**
             * Format: double
             * @description 当前主频，MHz。会随调频变化，仅供展示。
             * @example 2794.7
             */
            mhz?: number | null;
            /**
             * @description 型号名。异构机器（big.LITTLE）取第一个核的。
             * @example AMD EPYC 7543 32-Core Processor
             */
            model: string;
            /**
             * Format: int32
             * @description NUMA 节点数。读不到为 `None`。
             */
            numa_nodes?: number | null;
            /**
             * @description 物理封装（socket）到逻辑处理器的映射（roadmap/08 §5.2）。
             *     逻辑处理器视图按封装分块展示；读不到 topology 时为**一个**封装含全部核。
             */
            packages?: components["schemas"]["CpuPackage"][];
            /**
             * Format: int32
             * @description **物理**核数。无法可靠推断时为 `None`（不要用 `logical_cores` 顶替）。
             * @example 8
             */
            physical_cores?: number | null;
            /**
             * Format: double
             * @description cgroup 配额换算出的可用 CPU 数（容器里 `cpu.max` / `cpu.cfs_quota_us`）。
             *     无配额时为 `None`。有值且小于 `logical_cores` 时前端应按它算百分比。
             * @example 2
             */
            quota_cores?: number | null;
            /**
             * @description 厂商标识（`vendor_id`）。ARM 上常缺失，为 `None`。
             * @example AuthenticAMD
             */
            vendor?: string | null;
        };
        /** @description 一个物理封装（CPU socket / die）及其逻辑处理器（roadmap/08 §5.2）。 */
        CpuPackage: {
            /**
             * Format: int32
             * @description 物理封装 id，来自 `topology/physical_package_id`。
             * @example 0
             */
            id: number;
            /** @description 属于该封装的逻辑处理器编号，升序。 */
            logical_cores?: number[];
        };
        /** @description `POST /api/v1/nodes` 的请求体。 */
        CreateNodeReq: {
            /**
             * @description 节点 id。缺省由服务端生成随机 id。Agent 配置里的 `node_id` 必须等于它。
             * @example web-01
             */
            id?: string | null;
            /**
             * @description 显示名。
             * @example Web 服务器 1
             */
            name: string;
        };
        /** @description `POST /api/v1/nodes` 的响应体。 */
        CreateNodeResp: {
            /** @description 节点 id。 */
            id: string;
            /**
             * @description 预共享 token。**只在本响应里出现一次**，服务端只存 hash；
             *     抄进 Agent 配置的 `token` 字段。
             */
            token: string;
        };
        /** @description `POST /api/v1/terminals` 的请求体。 */
        CreateTerminalReq: {
            /**
             * @description 要启动的 shell 的绝对路径。缺省用目标用户 passwd 项里的 shell，
             *     再回落到 `/bin/sh`。路径不存在返回 [`crate::ErrorCode::InvalidRequest`]。
             * @example /bin/bash
             */
            shell?: string | null;
            /**
             * @description 以哪个用户身份运行。缺省为当前会话的登录用户（即 user worker）。
             *
             *     传别的用户（典型是 `"root"`）会路由到 **admin worker**，因此**要求会话已提权**；
             *     未提权时返回 [`crate::ErrorCode::ElevationRequired`]。
             * @example root
             */
            user?: string | null;
        };
        /** @description `POST /api/v1/terminals` 的响应体。 */
        CreateTerminalResp: {
            /**
             * @description 终端 id，用于拼 `WS /ws/terminal/{id}` 以及后续的 resize / delete。
             *     **仅在本浏览器会话内有效**，别的会话拿到也用不了。
             * @example t_7f3a91
             */
            id: string;
        };
        /** @description 目录项。 */
        DirEntryInfo: {
            /**
             * Format: int32
             * @description 属组 gid。
             */
            gid: number;
            /** @description 属组名。同上。 */
            group?: string | null;
            /** @description 类型。 */
            kind: components["schemas"]["FileKind"];
            /**
             * Format: int32
             * @description 权限位，八进制低 12 位（如 `0o644` = 420）。
             * @example 420
             */
            mode: number;
            /**
             * Format: int64
             * @description 最后修改时刻。
             */
            mtime_ts: number;
            /**
             * @description 条目名，不含路径。
             * @example hosts
             */
            name: string;
            /**
             * Format: int64
             * @description 大小，字节。目录为其自身的 inode 大小，无实际意义。
             * @example 221
             */
            size_bytes: number;
            /** @description 符号链接的目标；`kind != symlink` 时为 `None`。不解引用，原样返回。 */
            target?: string | null;
            /**
             * Format: int32
             * @description 属主 uid。
             */
            uid: number;
            /** @description 属主用户名。NSS 不可用时为 `None`（见 `docs/design.md` §10）。 */
            user?: string | null;
        };
        /** @description `GET /api/v1/files` 的响应体。 */
        DirListing: {
            /** @description 目录项。服务端已按「目录在前，其余按名称」排序；前端可以再排。 */
            entries?: components["schemas"]["DirEntryInfo"][];
            /**
             * @description 被列出的目录的规范化绝对路径。
             * @example /etc
             */
            path: string;
            /**
             * Format: int32
             * @description `lstat` 失败而被跳过的条目数（无权限、`/proc` 里的竞态消失）。
             *     为 0 表示全部列出。
             */
            skipped?: number;
        };
        /** @description 块设备（`/sys/block/<name>`）。 */
        DiskInfo: {
            /**
             * @description 设备型号（`device/model`）。虚拟设备读不到，为 `None`。
             * @example SAMSUNG MZQL21T9HCJR-00A07
             */
            model?: string | null;
            /**
             * @description 内核设备名，不带 `/dev/` 前缀。
             * @example nvme0n1
             */
            name: string;
            /** @description 是否只读。 */
            read_only: boolean;
            /** @description 是否可移动介质（`removable == 1`）。 */
            removable: boolean;
            /** @description 是否机械盘（`queue/rotational == 1`）。 */
            rotational: boolean;
            /**
             * Format: int64
             * @description 容量，字节（`size` × 512）。
             * @example 1920383410176
             */
            size_bytes: number;
            /**
             * @description SMART 总体健康结论。**P0 通常为 `None`**——判定 SMART 需要 root + `smartctl`，
             *     而本项目默认「系统里什么都没有」。有值时同时会出现在 [`HealthReport`] 里。
             */
            smart_healthy?: boolean | null;
        };
        /**
         * @description 错误码。这是**稳定的机器可读契约**，前端按它分支；`message` 只用于展示，不要拿去做判断。
         * @enum {string}
         */
        ErrorCode: "unauthenticated" | "elevation_required" | "permission_denied" | "capability_unavailable" | "not_found" | "invalid_request" | "conflict" | "timeout" | "unavailable" | "internal";
        /** @description 一个打开的文件描述符。 */
        FdInfo: {
            /**
             * Format: int32
             * @description fd 编号。
             * @example 3
             */
            fd: number;
            /**
             * @description 归类后的类型：`file` / `socket` / `pipe` / `anon_inode` / `dir` / `other`。
             * @example file
             */
            kind: string;
            /**
             * @description 软链目标：文件路径，或 `socket:[12345]` / `pipe:[678]` / `anon_inode:[eventpoll]`。
             * @example /var/log/nginx/access.log
             */
            target: string;
        };
        /** @description `GET /api/v1/files/content` 的响应体。 */
        FileContent: {
            /**
             * @description 文件内容。
             *
             *     只支持文本；二进制文件（前 8 KiB 含 NUL 字节）返回
             *     [`crate::ErrorCode::InvalidRequest`]（P0 不做下载与十六进制视图）。
             */
            content: string;
            /** @description 内容里是否有无效 UTF-8 序列被替换成 U+FFFD。为 `true` 时不宜原样写回。 */
            lossy?: boolean;
            /**
             * @description 规范化绝对路径。
             * @example /etc/hosts
             */
            path: string;
            /**
             * Format: int64
             * @description 文件大小，字节。
             */
            size_bytes: number;
            /**
             * @description 内容是否被截断。**当前恒为 `false`**：超过大小上限（5 MiB）的文件直接
             *     返回 [`crate::ErrorCode::InvalidRequest`] 而不是截断——半个文件比报错更
             *     误导。字段保留给后续的分段读取。
             */
            truncated: boolean;
        };
        /**
         * @description 文件类型。
         * @enum {string}
         */
        FileKind: "file" | "dir" | "symlink" | "block_device" | "char_device" | "fifo" | "socket" | "unknown";
        /** @description 已挂载文件系统的容量占用。 */
        FilesystemInfo: {
            /**
             * Format: int64
             * @description 非特权用户可用容量，字节（`statvfs.f_bavail`）。展示「还剩多少」用它。
             */
            available_bytes: number;
            /**
             * @description 承载它的**整盘**块设备名（不带 `/dev/`），如 `nvme0n1` / `dm-0`
             *     （roadmap/08 §5.1）。伪文件系统（tmpfs / nfs / overlay）与读不到 major:minor
             *     时为 `None`——界面显示「无块设备」，速率列显示 `—`。前端据此把同一块盘上的
             *     多个挂载点的读写按**去重后的设备**求和，避免把同一份 IO 数多遍。
             * @example nvme0n1
             */
            backing_dev?: string | null;
            /**
             * @description 源设备或伪文件系统名。
             * @example /dev/nvme0n1p2
             */
            device: string;
            /**
             * @description 文件系统类型。
             * @example ext4
             */
            fs_type: string;
            /**
             * Format: int64
             * @description inode 总数。不支持 inode 概念的文件系统（btrfs / tmpfs 部分实现）为 `None`。
             */
            inodes_total?: number | null;
            /**
             * Format: int64
             * @description 已用 inode 数。
             */
            inodes_used?: number | null;
            /**
             * @description 挂载点。
             * @example /
             */
            mount_point: string;
            /** @description 是否以只读挂载。 */
            read_only: boolean;
            /**
             * Format: int64
             * @description 总容量，字节。
             */
            total_bytes: number;
            /**
             * Format: int64
             * @description 已用容量，字节。等于 `total - free`，**含 root 保留块**（所以 `used + available < total`）。
             */
            used_bytes: number;
        };
        /** @description 一张显卡（roadmap/08 §5.3）。**只描述拓扑**，实时指标走 `gpu.*` series。 */
        GpuInfo: {
            /**
             * @description PCI 地址（`0000:62:00.0`）。虚拟 / 平台设备为 `None`。
             * @example 0000:62:00.0
             */
            bus?: string | null;
            /**
             * @description DRM 卡名，如 `card0`。与 `gpu.*` 指标的 `gpu` 标签一致。
             * @example card0
             */
            card: string;
            /**
             * @description 内核驱动（`device/driver` 的名字）：`amdgpu` / `i915` / `nvidia` /
             *     `nouveau` / `ast` / `mgag200` …
             * @example amdgpu
             */
            driver?: string | null;
            /**
             * @description 型号串。sysfs 没有营销名，这里给「厂商名 [vendor:device]」这类由 PCI id
             *     组出的可读串（前端可再经 pci.ids 美化）；认不出为 `None`。
             * @example AMD [1002:73a3]
             */
            model?: string | null;
            /** @description 指标数据源，见 [`GpuSource`]。 */
            source: components["schemas"]["GpuSource"];
            /**
             * Format: int64
             * @description 显存字节数（`device/mem_info_vram_total`，多为 amdgpu）。读不到为 `None`。
             */
            vram_bytes?: number | null;
        };
        /**
         * @description 显卡指标的数据来源（roadmap/08 §5.3）。决定这张卡哪些 `gpu.*` 指标可用。
         * @enum {string}
         */
        GpuSource: "sysfs" | "nvml" | "unavailable";
        /** @description DMI 硬件信息（`/sys/class/dmi/id/`）。字段读不到时为 `None`——容器与多数 ARM 板子都读不到。 */
        HardwareInfo: {
            /** @description BIOS / 固件版本。 */
            bios_version?: string | null;
            /**
             * @description `product_name`。
             * @example PowerEdge R650
             */
            product?: string | null;
            /** @description 机器序列号。**读取需要 root**，非特权进程通常拿不到，为 `None`。 */
            serial?: string | null;
            /**
             * @description `sys_vendor`。
             * @example Dell Inc.
             */
            vendor?: string | null;
        };
        /** @description 一条健康检查结果。 */
        HealthItem: {
            /**
             * @description 展开后的详细说明；没有更多可说时为 `None`。
             * @example nginx.service, redis.service, backup.timer
             */
            detail?: string | null;
            /**
             * @description 稳定的机器可读 id，同一类问题共用同一个 id：
             *     `"unit.failed"` / `"disk.usage"` / `"disk.inodes"` / `"disk.smart"` /
             *     `"reboot.required"` / `"fs.read_only"`。前端按它决定图标与跳转目标。
             * @example unit.failed
             */
            id: string;
            /** @description 严重级别。 */
            severity: components["schemas"]["HealthSeverity"];
            /**
             * @description 具体对象，供前端做深链：unit 名 / 挂载点 / 块设备名。
             *     同一个 `id` 可以出现多条，靠 `target` 区分（比如两个挂载点都超阈值）。
             * @example /var
             */
            target?: string | null;
            /**
             * @description 一句话标题，可直接展示。
             * @example 3 个 unit 处于 failed 状态
             */
            title: string;
        };
        /**
         * @description `GET /api/v1/system/health` 的响应体：结构化健康条目列表。
         *
         *     不返回自由文本——每条都带稳定的 [`HealthItem::id`]，前端据此做本地化、去重与跳转。
         */
        HealthReport: {
            /** @description 条目列表，建议按 `severity` 降序排列。**空数组表示一切正常**，不是「没检查」。 */
            items?: components["schemas"]["HealthItem"][];
            /**
             * @description 因能力缺失而**未能检查**的项，取值为能力名（如 `"systemd"`、`"journal"`）。
             *     前端应说明「未检查」而非「正常」。
             */
            skipped?: string[];
            /** @description 总体结论 = 所有条目里最高的严重级别；无条目时为 [`HealthSeverity::Ok`]。 */
            status: components["schemas"]["HealthSeverity"];
            /**
             * Format: int64
             * @description 采样时刻。
             */
            ts: number;
        };
        /** @description 健康检查响应。 */
        HealthResponse: {
            /**
             * @description 固定为 `ok`；服务起不来时根本不会有响应，所以这里没有别的取值。
             * @example ok
             */
            status: string;
            /**
             * Format: int64
             * @description 进程已运行秒数。
             * @example 42
             */
            uptime_secs: number;
            /**
             * @description 二进制版本（`CARGO_PKG_VERSION`）。
             * @example 0.1.0
             */
            version: string;
        };
        /**
         * @description 健康条目的严重级别。
         * @enum {string}
         */
        HealthSeverity: "ok" | "info" | "warning" | "critical";
        /** @description 未认证即可见的主机身份（[`Capabilities::identity`]）。启动时从 host provider 取一次。 */
        HostIdentity: {
            /**
             * @description 主机名。
             * @example strix-web-01
             */
            hostname: string;
            /**
             * @description 内核版本（`uname -r`）。
             * @example 6.8.0-45-generic
             */
            kernel: string;
            /**
             * @description os-release 的 `ID`（`"ubuntu"` / `"arch"` / …），前端据此选发行版主题色；
             *     认不出时为空字符串，前端回落到中性灰。
             * @example ubuntu
             */
            os_id: string;
            /**
             * @description os-release 的 `PRETTY_NAME`，直接展示。
             * @example Ubuntu 24.04.2 LTS
             */
            os_name: string;
        };
        /**
         * @description 一条日志（列表视图所需的字段）。
         *
         *     # 时间精度
         *
         *     journald 的 `__REALTIME_TIMESTAMP` 是**微秒级 epoch**，这里拆成两个字段：
         *     [`Self::ts`]（unix 秒，遵循全局约定）+ [`Self::us`]（秒内微秒偏移）。
         *
         *     日志是全局「时间戳一律 i64 秒」约定的**唯一例外**：一个服务吐 stack trace 时
         *     同一秒内几十条是常态，只有秒精度的话时间列会显示成一片相同的值。
         *
         *     `us` **只服务于展示**；排序与定位仍然靠 [`Self::cursor`]。
         */
        LogEntry: {
            /** @description 所属 boot id（`_BOOT_ID`）。 */
            boot_id?: string | null;
            /**
             * @description journald 游标（`__CURSOR`），不透明字符串。用于翻页、以及
             *     `GET /api/v1/logs/entry/{cursor}` 取全字段详情（作为路径参数需 URL 编码）。
             * @example s=1a2b;i=3c4d;b=5e6f;m=7089;t=90ab;x=cdef
             */
            cursor: string;
            /** @description 主机名（`_HOSTNAME`）。多节点汇聚时用于区分来源。 */
            hostname?: string | null;
            /**
             * @description syslog 标识（`SYSLOG_IDENTIFIER`），通常是可执行文件名。
             * @example nginx
             */
            identifier?: string | null;
            /**
             * @description 消息正文（`MESSAGE`）。二进制消息会被转成有损的可打印形式。
             * @example Started A high performance web server.
             */
            message: string;
            /**
             * Format: int32
             * @description 产生消息的进程 pid（`_PID`）。内核消息为 `None`。
             */
            pid?: number | null;
            /** @description 优先级。原始记录缺 `PRIORITY` 时按 [`LogPriority::Info`] 归一。 */
            priority: components["schemas"]["LogPriority"];
            /**
             * @description 传输方式（`_TRANSPORT`）：`journal` / `syslog` / `kernel` / `stdout` / `audit`。
             * @example stdout
             */
            transport?: string | null;
            /**
             * Format: int64
             * @description 记录时刻，unix 秒（`__REALTIME_TIMESTAMP / 1_000_000`）。
             * @example 1756252800
             */
            ts: number;
            /**
             * Format: int32
             * @description 产生消息的进程 uid（`_UID`）。
             */
            uid?: number | null;
            /**
             * @description 来源 unit（`_SYSTEMD_UNIT`，内核消息回落到 `UNIT`）。内核与早期用户态消息为 `None`。
             * @example nginx.service
             */
            unit?: string | null;
            /**
             * Format: int32
             * @description 秒内微秒偏移，范围 `0..1_000_000`（`__REALTIME_TIMESTAMP % 1_000_000`）。
             *
             *     完整时刻 = `ts` 秒 + `us` 微秒。仅用于展示（毫秒/微秒列），
             *     **不要拿它排序**——同一微秒内仍可能有多条，顺序以数组顺序与 `cursor` 为准。
             * @example 481237
             */
            us: number;
        };
        /** @description `GET /api/v1/logs/entry/{cursor}` 的响应体：单条日志的**全字段**详情。 */
        LogEntryDetail: components["schemas"]["LogEntry"] & {
            /**
             * @description journald 的**全部**原始字段，键为 `_PID` / `_CMDLINE` / `CODE_FILE` 等。
             *     二进制值会被跳过或做有损转换。键集合完全取决于写日志的程序，前端按 k-v 表格展示即可。
             */
            fields?: Record<string, never>;
        };
        /** @description `GET /api/v1/logs` 的响应体：一页日志 + 游标。 */
        LogPage: {
            /** @description 本页条目，**由新到旧**排序。 */
            entries?: components["schemas"]["LogEntry"][];
            /**
             * @description 继续向**更旧**方向翻页的游标（= 本页最后一条的 `cursor`）。
             *     为 `None` 表示已到最旧一条。
             */
            next_cursor?: string | null;
            /**
             * @description 向**更新**方向翻页的游标（= 本页第一条的 `cursor`）。
             *     为 `None` 表示本页为空。用于「回到更新的日志」与增量刷新。
             */
            prev_cursor?: string | null;
        };
        /**
         * @description syslog 优先级，取值与数字 0–7 一一对应（0 最严重）。
         * @enum {string}
         */
        LogPriority: "emerg" | "alert" | "crit" | "err" | "warning" | "notice" | "info" | "debug";
        /** @description `GET /api/v1/logs/usage` 的响应体：日志磁盘占用与可用的清理方式。 */
        LogUsage: {
            /**
             * Format: int64
             * @description 日志占用的磁盘字节数。`None` = 测不到（macOS 的日志库目录对非 root 不可读）。
             * @example 58720256
             */
            bytes?: number | null;
            /** @description 本机支持的清理方式。空数组 = 完全不支持清理。 */
            modes: components["schemas"]["VacuumMode"][];
        };
        /** @description 内存与 swap 容量（`/proc/meminfo`）。这里只放**容量**，实时使用率走指标接口。 */
        MemoryInfo: {
            /**
             * Format: int64
             * @description `MemAvailable`，字节。这是「还能给新进程用多少」，**不是** `free`——展示占用率请用它。
             * @example 41943040000
             */
            available_bytes: number;
            /**
             * Format: int64
             * @description `SwapFree`，字节。
             * @example 0
             */
            swap_free_bytes: number;
            /**
             * Format: int64
             * @description `SwapTotal`，字节。无 swap 时为 0。
             * @example 0
             */
            swap_total_bytes: number;
            /**
             * Format: int64
             * @description `MemTotal`，字节。
             * @example 67108864000
             */
            total_bytes: number;
        };
        /**
         * @description 落盘的聚合层。`step` 与桶宽的对应关系见 `docs/design.md` §7.2。
         * @enum {string}
         */
        MetricLayer: "live" | "m_1m" | "m_5m" | "m_15m" | "m_12h" | "m_1d";
        /** @description 一个聚合桶。五个字段的语义与 `docs/design.md` §7.2 的表结构逐一对应。 */
        MetricPoint: {
            /**
             * Format: int32
             * @description 该桶内的实际采样点数。**用于缺失检测**：明显小于 `bucket_secs / 采集间隔` 说明有丢采。
             *     也是算 `avg = sum / cnt` 的分母。
             * @example 30
             */
            cnt: number;
            /**
             * Format: double
             * @description 桶内最大值。
             * @example 88.4
             */
            max: number;
            /**
             * Format: double
             * @description 桶内中位数。`m_1m` 层为真中位数，更粗的层为 median of medians（近似值）。
             * @example 18.7
             */
            med: number;
            /**
             * Format: double
             * @description 桶内最小值。
             * @example 1.2
             */
            min: number;
            /**
             * Format: double
             * @description 桶内**总和**，不是平均值。`avg = sum / cnt`，由前端计算。
             * @example 620.5
             */
            sum: number;
            /**
             * Format: int64
             * @description **桶起始**时刻，unix 秒（不是桶中点，也不是结束时刻）。
             * @example 1756252800
             */
            ts: number;
        };
        /** @description `GET /api/v1/metrics/query` 的响应体。 */
        MetricQueryResp: {
            /**
             * Format: int64
             * @description 实际使用的起始时刻（可能被对齐到桶边界，因此不一定等于请求的 `from`）。
             */
            from: number;
            /** @description 实际选中的层。前端应展示它——用户需要知道自己看的是 1 分钟还是 1 天粒度。 */
            layer: components["schemas"]["MetricLayer"];
            /** @description 各序列结果。请求里存在但库中查无此序列的，**不会**出现在这里（不是返回空 points）。 */
            series?: components["schemas"]["MetricSeries"][];
            /**
             * Format: int32
             * @description 实际点间隔，秒，等于 `layer.bucket_secs()`（`live` 层为实际采集间隔）。
             * @example 60
             */
            step: number;
            /**
             * Format: int64
             * @description 实际使用的结束时刻。
             */
            to: number;
        };
        /** @description 一条序列的查询结果。 */
        MetricSeries: {
            /** @description 序列元信息。 */
            meta: components["schemas"]["SeriesMeta"];
            /**
             * @description 桶数组，按 `ts` 升序。
             *
             *     **有洞就是真的没数据**（服务未启动 / 该期间已过保留期），服务端不做零填充，
             *     前端应断线而不是连成直线。
             */
            points?: components["schemas"]["MetricPoint"][];
        };
        /** @description `GET /api/v1/metrics/current` 的响应体，同时也是 WS `metrics.live` 频道推送的 payload。 */
        MetricSnapshot: {
            /**
             * Format: int64
             * @description 采样时刻。
             */
            ts: number;
            /** @description 本次采样的全部瞬时值。 */
            values?: components["schemas"]["MetricValue"][];
        };
        /**
         * @description 一个瞬时值。
         *
         *     这里刻意**不带 `series.id`**：快照可能包含尚未落库、因而还没有 id 的序列。
         *     前端按 `metric` + `labels` 与 [`SeriesMeta`] 对齐。
         */
        MetricValue: {
            /**
             * @description 标签，拼法同 [`SeriesMeta::labels`]；无标签为空串。
             * @example
             */
            labels?: string;
            /**
             * @description 指标名，同 [`SeriesMeta::metric`]。
             * @example cpu.usage
             */
            metric: string;
            /**
             * @description 单位，取值同 [`SeriesMeta::unit`]。
             * @example percent
             */
            unit?: string | null;
            /**
             * Format: double
             * @description 瞬时值。速率类指标（`*_bytes` / `*.iops`）已由服务端做过差分，
             *     单位见 [`Self::unit`]，前端不要再自己求差。
             * @example 23.7
             */
            value: number;
        };
        /** @description 一个网络接口（roadmap/08 §5.4）。**只描述拓扑**，实时吞吐走 `net.*` series。 */
        NetInfo: {
            /** @description 该接口上配置的 IP 地址（v4 与 v6）。 */
            addrs?: string[];
            /** @description 是否有载波（`carrier`），即物理链路是否连通。 */
            carrier: boolean;
            /**
             * @description 内核驱动（`device/driver` 的名字）。虚拟接口为 `None`。
             * @example e1000e
             */
            driver?: string | null;
            /**
             * @description 双工（`duplex`）：`full` / `half`；`unknown` 与读不到均为 `None`。
             * @example full
             */
            duplex?: string | null;
            /**
             * @description MAC 地址（`address`）。无硬件地址的接口为 `None`。
             * @example aa:bb:cc:dd:ee:ff
             */
            mac?: string | null;
            /**
             * Format: int32
             * @description MTU（`mtu`）。
             * @example 1500
             */
            mtu: number;
            /**
             * @description 接口名。与 `net.*` 指标的 `iface` 标签一致。
             * @example eth0
             */
            name: string;
            /**
             * Format: int32
             * @description 链路速率，Mb/s（`speed`）。虚拟网卡、bond、无线、断链常返回 -1 → `None`。
             *     唯一用途是把吞吐归一成链路利用率；读不到不影响任何功能。
             * @example 10000
             */
            speed_mbps?: number | null;
        };
        /** @description `GET /api/v1/nodes` 的列表项。**绝不包含 token 或其 hash**。 */
        NodeInfo: {
            /**
             * Format: int64
             * @description 登记时间。
             */
            created_ts: number;
            /**
             * @description 节点 id：`"local"` 或登记时指定 / 生成的标识。
             * @example web-01
             */
            id: string;
            /**
             * @description `"local"` 或 `"agent"`。
             * @example agent
             */
            kind: string;
            /**
             * Format: int64
             * @description 最近一次心跳（unix 秒）。从未连接过为 `None`。
             */
            last_seen?: number | null;
            /**
             * @description 显示名。
             * @example Web 服务器 1
             */
            name: string;
            /** @description 此刻是否有存活的 Agent 连接。`local` 恒为 `true`。 */
            online: boolean;
        };
        /** @description 发行版信息，来自 `/etc/os-release`。 */
        OsInfo: {
            /**
             * @description `ID`，如 `"ubuntu"` / `"debian"` / `"fedora"`。用于安装期选 PAM 模板等分支判断。
             * @example ubuntu
             */
            id: string;
            /**
             * @description `NAME`。
             * @example Ubuntu
             */
            name: string;
            /**
             * @description `PRETTY_NAME`，直接用于展示。
             * @example Ubuntu 24.04.2 LTS
             */
            pretty_name: string;
            /**
             * @description `VERSION_ID`。滚动发行版（Arch 等）没有此字段，为 `None`。
             * @example 24.04
             */
            version?: string | null;
        };
        /**
         * @description 电源操作。
         * @enum {string}
         */
        PowerAction: "reboot" | "poweroff";
        /**
         * @description `POST /api/v1/system/power` 的请求体。
         *
         *     该操作必然要求管理访问；未提权时返回 [`crate::ErrorCode::ElevationRequired`]。
         */
        PowerReq: {
            /** @description 要执行的操作。 */
            action: components["schemas"]["PowerAction"];
        };
        /** @description `GET /api/v1/processes/{pid}` 的响应体。 */
        ProcessDetail: components["schemas"]["ProcessSummary"] & {
            /**
             * @description cgroup 路径（`/proc/<pid>/cgroup` 的 v2 行）。
             * @example /system.slice/nginx.service
             */
            cgroup?: string | null;
            /** @description 命令行参数数组（未拼接的原始形式，`argv[0]` 在首位）。内核线程为空数组。 */
            cmdline_args?: string[];
            /** @description 当前工作目录。无权限时为 `None`。 */
            cwd?: string | null;
            /**
             * @description 环境变量（⭕ 可选项）。
             *
             *     **需要同 uid 或 root** 才能读，无权限时为 `None`（而不是空 map）。
             *     环境变量里常含密钥，前端展示前应默认折叠。
             */
            environ?: Record<string, never>;
            /**
             * Format: int32
             * @description 有效 uid（`/proc/<pid>/status` 的 `Uid` 第二列）。setuid 程序会与 `summary.uid` 不同。
             */
            euid?: number | null;
            /**
             * @description 可执行文件真实路径（`/proc/<pid>/exe` 的软链目标）。
             *     **无权限或内核线程时为 `None`**；文件已被删除时路径会带 `" (deleted)"` 后缀。
             * @example /usr/sbin/nginx
             */
            exe?: string | null;
            /** @description 打开的文件描述符（⭕ 可选项）。无权限时为 `None`。 */
            fds?: components["schemas"]["FdInfo"][] | null;
            /**
             * Format: int32
             * @description 实际 gid。
             */
            gid?: number | null;
            /**
             * Format: int64
             * @description 累计读取字节数（`/proc/<pid>/io` 的 `read_bytes`）。无权限时为 `None`。
             */
            io_read_bytes?: number | null;
            /**
             * Format: int64
             * @description 累计写入字节数（`write_bytes`）。无权限时为 `None`。
             */
            io_write_bytes?: number | null;
            /** @description 控制终端（`/proc/<pid>/stat` 的 `tty_nr` 解出的设备名，如 `pts/0`）。无终端为 `None`。 */
            tty?: string | null;
            /**
             * @description **所属 systemd unit**，由 `cgroup` 路径反查得出（`docs/design.md` §13 步骤 16）。
             *     不在任何 unit 下（内核线程、手工 `nohup`）时为 `None`。有值时前端应链到服务详情页。
             * @example nginx.service
             */
            unit?: string | null;
        };
        /**
         * @description 进程列表的排序字段。
         * @enum {string}
         */
        ProcessSortKey: "pid" | "name" | "cpu" | "mem" | "user" | "start_ts";
        /**
         * @description 进程状态（`/proc/<pid>/stat` 的第 3 个字段）。
         * @enum {string}
         */
        ProcessState: "running" | "sleeping" | "disk_sleep" | "zombie" | "stopped" | "tracing_stop" | "dead" | "idle" | "unknown";
        /** @description `GET /api/v1/processes` 的列表项。 */
        ProcessSummary: {
            /**
             * @description 完整命令行，参数间以空格连接（原始的 NUL 分隔已展开）。
             *     内核线程无 cmdline，为 `None`——这是区分内核线程与用户进程的可靠依据。
             * @example nginx: worker process
             */
            cmdline?: string | null;
            /**
             * Format: double
             * @description 最近一个采样窗口内的 CPU 占用，**百分比**。单核跑满 = 100.0，多核可超过 100.0。
             *
             *     需要两次采样求差，**首次请求可能为 0.0**（还没有前一次样本）。
             * @example 12.5
             */
            cpu_percent: number;
            /**
             * Format: double
             * @description 磁盘读速率（bytes/s，最近一个采样窗口的差分）。
             *
             *     `None` = **测不到**（无权限，或平台不提供 per-process IO 计数）；
             *     能测到但还没有第二轮采样时为 `Some(0.0)`。前端把 `None` 画成「—」而不是 0。
             */
            io_read_rate?: number | null;
            /**
             * Format: double
             * @description 磁盘写速率（bytes/s）。语义同 [`Self::io_read_rate`]。
             */
            io_write_rate?: number | null;
            /**
             * Format: double
             * @description 内存占用比例，**百分比**，= `rss_bytes / MemTotal × 100`。
             * @example 0.4
             */
            mem_percent: number;
            /**
             * @description 短名（`/proc/<pid>/comm`），最长 15 字节，会被内核截断。
             * @example nginx
             */
            name: string;
            /**
             * Format: int32
             * @description nice 值，范围 -20..=19。越小优先级越高。
             * @example 0
             */
            nice: number;
            /**
             * Format: int32
             * @description 进程 id。
             * @example 1234
             */
            pid: number;
            /**
             * Format: int32
             * @description 父进程 id。前端用 `pid`/`ppid` 自行拼树（响应本身始终是平铺数组）。
             * @example 1
             */
            ppid: number;
            /**
             * Format: int64
             * @description 常驻内存，字节（RSS）。共享页会被重复计入多个进程，**不要求和**。
             * @example 268435456
             */
            rss_bytes: number;
            /**
             * Format: int64
             * @description 进程启动时刻，unix 秒（由 `/proc/<pid>/stat` 的 `starttime` + boot 时间换算）。
             */
            start_ts: number;
            /** @description 进程状态。 */
            state: components["schemas"]["ProcessState"];
            /**
             * Format: int32
             * @description 线程数。
             * @example 4
             */
            threads: number;
            /**
             * Format: int32
             * @description 实际 uid。
             * @example 33
             */
            uid: number;
            /**
             * @description uid 对应的用户名。静态 musl 下 NSS 不可用时可能解析不出，为 `None`
             *     （见 `docs/design.md` §10：NSS 代理是 helper 的 P1 职责）。
             * @example www-data
             */
            user?: string | null;
            /**
             * Format: int64
             * @description 虚拟内存，字节（VSZ）。含未映射实页的地址空间，通常远大于实际占用，仅供参考。
             * @example 1073741824
             */
            vms_bytes: number;
        };
        /** @description 一条 PAM 提示。 */
        Prompt: {
            /**
             * Format: int32
             * @description 本轮内唯一的序号，回应时按此 id 对应。**只在同一个 `session` 的同一轮内有效**，
             *     下一轮 id 会重新编号。
             * @example 0
             */
            id: number;
            /** @description 展示风格，决定是否需要输入、是否回显。 */
            style: components["schemas"]["PromptStyle"];
            /**
             * @description PAM 给出的原文，可能是任意语言（取决于 PAM 模块与系统 locale），前端**原样展示**，
             *     不要试图匹配内容做逻辑。
             * @example Password:
             */
            text: string;
        };
        /**
         * @description 一条提示的回应。
         *
         *     # 安全
         *
         *     **不实现 `Serialize` / `Clone`**，`Debug` 脱敏，`value` 由 [`Zeroizing`] 保护。见模块文档。
         *
         *     下面这段**必须编译不过**——它是「凭据不可被序列化」这条约束的可执行证明：
         *
         *     ```compile_fail
         *     use strixmaid_types::auth::PromptResponse;
         *     fn leak(r: &PromptResponse) -> String {
         *         serde_json::to_string(r).unwrap()
         *     }
         *     ```
         */
        PromptResponse: {
            /**
             * Format: int32
             * @description 对应的 [`Prompt::id`]。
             * @example 0
             */
            id: number;
            /**
             * @description 用户输入的原文，**drop 时自动 zeroize**。
             *
             *     # ⚠️ 敏感数据
             *
             *     当 [`Prompt::style`] 为 [`PromptStyle::Prompt`] 时这就是**明文密码**。
             *
             *     - **禁止进日志**，包括 `debug` / `trace` 级——注意 `Zeroizing` 自己的 `Debug`
             *       会原样打印内层明文，所以外层结构的 `Debug` 必须手写脱敏；
             *     - **禁止入库**、禁止进审计记录、禁止出现在错误 `message` / `detail` 里；
             *     - 往下传时 **move**，不要 `clone()` 出一份不受保护的拷贝；
             *     - 不要 `to_string()` / `as_str().to_owned()`——那会复制出一份逃出 `Zeroizing` 的明文。
             *
             *     JSON 上它就是普通字符串，因此 schema 标注为 `String`。
             * @example <sensitive>
             */
            value: string;
        };
        /**
         * @description PAM 提示的展示风格，对应 `PAM_PROMPT_ECHO_OFF` / `PAM_PROMPT_ECHO_ON` /
         *     `PAM_TEXT_INFO` / `PAM_ERROR_MSG`。
         * @enum {string}
         */
        PromptStyle: "prompt" | "prompt_echo" | "info" | "error";
        /** @description `POST /api/v1/processes/{pid}/renice` 的请求体（⭕ 可选项）。 */
        ReniceReq: {
            /**
             * Format: int32
             * @description 目标 nice 值，范围 -20..=19。**调低（更高优先级）需要 root**，
             *     非特权用户只能调高，否则返回 [`crate::ErrorCode::PermissionDenied`]。
             * @example 10
             */
            nice: number;
        };
        /**
         * @description `POST /api/v1/terminals/{id}/resize` 的请求体。
         *
         *     走 REST 而不是塞进终端 WS 流，是因为写操作一律走 REST（幂等、易调试、好审计），
         *     见 `docs/design.md` §9.1 末尾。
         */
        ResizeReq: {
            /**
             * Format: int32
             * @description 列数，必须 > 0。
             * @example 120
             */
            cols: number;
            /**
             * Format: int32
             * @description 行数，必须 > 0。
             * @example 32
             */
            rows: number;
        };
        /** @description 一条时间序列的元信息，对应 `docs/design.md` §8 的 `series` 表。 */
        SeriesMeta: {
            /**
             * Format: int64
             * @description `series.id`，数值主键。查询时用它比用 `metric+labels` 拼字符串快。
             * @example 42
             */
            id: number;
            /**
             * @description 标签，形如 `k=v` 并**按键排序后以 `,` 拼接**；无标签时为空串（不是 `null`）。
             *     这是 `series` 表 `UNIQUE(node, metric, labels)` 的一部分，拼法必须稳定。
             * @example dev=sda
             */
            labels: string;
            /**
             * @description 指标名，点分层级。
             * @example disk.read_bytes
             */
            metric: string;
            /**
             * @description 节点 id：`"local"` 或 Agent 的 uuid。
             * @example local
             */
            node: string;
            /**
             * @description 单位，供前端选择格式化方式：`"bytes"` / `"bytes/s"` / `"percent"` / `"count"` /
             *     `"seconds"` / `"iops"`。未标注时为 `None`，按裸数值展示。
             * @example bytes/s
             */
            unit?: string | null;
        };
        /**
         * @description `GET /api/v1/auth/session` 的响应体：当前浏览器会话在**当前节点**上的认证状态。
         *
         *     字段对应 `docs/design.md` §8 的 `sessions` + `node_sessions` 两张表。
         *     MVP 里 `node_sessions` 永远只有 `local` 一行，但类型从第一天起就带 `node`。
         */
        SessionInfo: {
            /**
             * Format: int64
             * @description 本节点上完成认证的时刻。
             * @example 1756252800
             */
            authed_ts: number;
            /**
             * Format: int64
             * @description 浏览器会话的创建时刻（`sessions.created_at`）。
             */
            created_ts: number;
            /** @description 是否已启用管理访问（admin worker 已就绪）。 */
            elevated: boolean;
            /**
             * Format: int64
             * @description 提权发生的时刻；`elevated == false` 时为 `None`。
             */
            elevated_ts?: number | null;
            /** @description 所属组名列表。 */
            groups?: string[];
            /**
             * Format: int64
             * @description 最近一次活跃时刻，用于空闲超时。
             */
            last_active_ts: number;
            /**
             * @description 节点 id。MVP 恒为 `"local"`。
             * @example local
             */
            node: string;
            /** @description 登录时记录的来源地址；经反向代理时可能不准或为 `None`。 */
            remote_addr?: string | null;
            /**
             * @description `pam_open_session` 是否成功——决定用户级 unit（`systemctl --user`）是否可用
             *     （design.md §5.4）。前端据此启用 / 隐藏 `scope=user`；也是 07 §1.2 #3 的验收点。
             */
            session_opened?: boolean;
            /**
             * Format: int32
             * @description 已认证的系统 uid。
             * @example 1000
             */
            uid: number;
            /** @description 登录时记录的 User-Agent；未采集到则为 `None`。 */
            user_agent?: string | null;
            /**
             * @description 已认证的系统用户名。
             * @example alice
             */
            username: string;
        };
        /** @description `PUT /api/v1/system/hostname` 的请求体（⭕ 可选项）。 */
        SetHostnameReq: {
            /**
             * @description 新的静态主机名。必须是合法 hostname（仅 `[a-zA-Z0-9-.]`，不超过 64 字节）。
             * @example web-02
             */
            hostname: string;
            /** @description 新的「漂亮主机名」，允许任意 UTF-8。传 `None` 表示不改动。 */
            pretty_hostname?: string | null;
        };
        /** @description `PUT /api/v1/system/timezone` 的请求体（⭕ 可选项）。 */
        SetTimezoneReq: {
            /**
             * @description IANA 时区名，必须存在于 `/usr/share/zoneinfo`。
             * @example Asia/Shanghai
             */
            timezone: string;
        };
        /**
         * @description 允许发送的信号。
         *
         *     只开放 `docs/design.md` §9.1 明确列出的三个——不做通用 `kill(2)` 网关，
         *     避免变成任意信号注入的工具。
         *
         *     序列化输出为小写（全局约定），同时以 `alias` 接受 design.md 里写的大写形式，
         *     两种拼写都能反序列化。
         * @enum {string}
         */
        SignalName: "term" | "kill" | "hup";
        /** @description `POST /api/v1/processes/{pid}/signal` 的请求体。 */
        SignalReq: {
            /** @description 要发送的信号。 */
            signal: components["schemas"]["SignalName"];
        };
        /**
         * @description 排序方向。
         * @enum {string}
         */
        SortOrder: "asc" | "desc";
        /** @description 机器级能力。字段名即 [`crate::ApiError::capability`] 的取值。 */
        SystemCapabilities: {
            /**
             * @description `strixmaid-helper` 已部署且 IPC socket 可用。
             *     为 `false` 时无法 PAM 认证、无法 setuid，登录与终端都不可用。
             * @example true
             */
            helper: boolean;
            /**
             * @description 有 journald（`journalctl` 可执行）。为 `false` 时隐藏日志页。
             * @example true
             */
            journal: boolean;
            /**
             * @description 检测到 podman。P0 不做容器管理，此位仅用于前端预留入口。
             * @example false
             */
            podman: boolean;
            /**
             * @description 系统上有 polkit。为 `false` 时提权只能靠「以 root 身份跑 admin worker」，
             *     且 systemd 操作的授权裁决退化为纯 uid 判断。
             * @example true
             */
            polkit: boolean;
            /**
             * @description 有 systemd（能连上 `org.freedesktop.systemd1`，或至少有可用的 `systemctl`）。
             *     为 `false` 时隐藏服务页。
             * @example true
             */
            systemd: boolean;
            /**
             * @description 支持用户级 unit（`systemd --user`）。前提是 helper 能 `pam_open_session` 并 setuid 后连 session bus。
             *     为 `false` 时服务页隐藏 `scope=user` 选项。
             * @example true
             */
            user_units: boolean;
        };
        /** @description `GET /api/v1/system/info` 的响应体。 */
        SystemInfo: {
            /**
             * @description CPU 架构（`uname -m`）。
             * @example x86_64
             */
            arch: string;
            /**
             * Format: int64
             * @description 开机时刻（`ts - uptime_secs`）。
             */
            boot_ts: number;
            /** @description CPU 信息。 */
            cpu: components["schemas"]["CpuInfo"];
            /** @description **块设备**列表（`/sys/block`）。不含分区，不含虚拟设备（loop / ram）。 */
            disks?: components["schemas"]["DiskInfo"][];
            /**
             * @description **已挂载文件系统**的容量占用（`/proc/self/mountinfo` + `statvfs`）。
             *     与 `disks` 是两件事：一块盘可以挂多个文件系统，也可以一个都不挂。
             */
            filesystems?: components["schemas"]["FilesystemInfo"][];
            /**
             * @description 显卡列表（roadmap/08 §5.3）。Linux 枚举 `/sys/class/drm/card*`，
             *     **任何驱动的卡都会被列出**（拓扑与驱动无关）；每张卡的 `source` 指明
             *     哪些指标可用。macOS 上为空（GPU 走 IOKit，P0 不做）。
             */
            gpus?: components["schemas"]["GpuInfo"][];
            hardware?: null | components["schemas"]["HardwareInfo"];
            /**
             * @description 静态主机名（`/etc/hostname` / `uname -n`）。
             * @example web-01
             */
            hostname: string;
            /**
             * @description 内核版本（`uname -r`）。
             * @example 6.8.0-71-generic
             */
            kernel: string;
            /** @description 机器唯一 id（`/etc/machine-id`）。读不到时为 `None`。 */
            machine_id?: string | null;
            /** @description 内存与 swap 容量。 */
            memory: components["schemas"]["MemoryInfo"];
            /**
             * @description 网络接口列表（roadmap/08 §5.4）。用于把吞吐归一成链路利用率、分组展示。
             *     排除 `lo` 与 `veth*`（与指标采集的口径一致）。
             */
            networks?: components["schemas"]["NetInfo"][];
            /** @description 发行版信息（`/etc/os-release`）。 */
            os: components["schemas"]["OsInfo"];
            /**
             * @description 可读的「漂亮主机名」（`/etc/machine-info` 的 `PRETTY_HOSTNAME`）。多数机器没有，为 `None`。
             * @example 生产 Web 节点 1
             */
            pretty_hostname?: string | null;
            /**
             * Format: int64
             * @description 采样时刻。
             */
            ts: number;
            /**
             * Format: int64
             * @description 开机时长，秒。
             * @example 864000
             */
            uptime_secs: number;
            /**
             * @description 虚拟化类型：`"kvm"` / `"vmware"` / `"docker"` / `"lxc"` / `"wsl"` 等，
             *     与 `systemd-detect-virt` 的取值一致。**物理机为 `None`**（而不是字符串 `"none"`）。
             * @example kvm
             */
            virtualization?: string | null;
        };
        /** @description `GET /api/v1/terminals` 的列表项：本会话当前存活的终端。 */
        TerminalInfo: {
            /**
             * @description 当前是否有 WS 连接挂在上面。
             *
             *     为 `false` 不等于终端已死——刷新页面后终端仍在，回看缓冲还留着
             *     （`docs/design.md` §13 步骤 23），重新连上即可继续。
             */
            attached: boolean;
            /**
             * Format: int32
             * @description 当前列数。
             * @example 120
             */
            cols: number;
            /**
             * Format: int64
             * @description 创建时刻。
             */
            created_ts: number;
            /**
             * @description 终端 id。
             * @example t_7f3a91
             */
            id: string;
            /**
             * Format: int64
             * @description 最近一次有字节流经过的时刻，用于空闲回收。
             */
            last_active_ts: number;
            /**
             * Format: int32
             * @description 当前行数。
             * @example 32
             */
            rows: number;
            /**
             * @description 实际启动的 shell 路径。
             * @example /bin/bash
             */
            shell: string;
            /**
             * Format: int32
             * @description 运行身份的 uid。`0` 表示这是一个提权终端，前端应显著标记。
             * @example 1000
             */
            uid: number;
            /**
             * @description 实际运行身份的用户名。
             * @example alice
             */
            user: string;
        };
        /** @description `GET /api/v1/system/time` 的响应体。 */
        TimeInfo: {
            /**
             * @description 是否启用了网络时间同步（`systemd-timesyncd` / `chronyd` / `ntpd` 已 enable）。
             *     探测不到任何 NTP 实现时为 `None`（区别于「明确没开」的 `Some(false)`）。
             */
            ntp_enabled?: boolean | null;
            /**
             * @description 提供 NTP 的服务名，用于前端展示与跳转到对应 unit。
             * @example systemd-timesyncd.service
             */
            ntp_service?: string | null;
            /** @description 时钟是否**已经**同步上。同 `ntp_enabled`，探测不到为 `None`。 */
            ntp_synchronized?: boolean | null;
            /** @description RTC 是否按本地时间而非 UTC 存储（双系统机器常见）。读不到为 `None`。 */
            rtc_local?: boolean | null;
            /**
             * @description IANA 时区名。读不到时为 `"UTC"`。
             * @example Asia/Shanghai
             */
            timezone: string;
            /**
             * Format: int64
             * @description 服务端当前时刻。前端可与本地时钟比对，提示「服务器时间偏差 N 秒」。
             */
            ts: number;
            /**
             * Format: int32
             * @description 当前时区相对 UTC 的偏移，**秒**（东为正）。含夏令时。
             * @example 28800
             */
            utc_offset_secs: number;
        };
        /** @description `GET /api/v1/services/timers` 列表项：一条定时任务。 */
        TimerEntry: {
            /**
             * @description 定时器当前是否生效（systemd: timer unit active；launchd: job 已加载）。
             *     `None` = 判定不了——非 root 读不到 launchd system 域的加载状态，不编（spec §6）。
             */
            active?: boolean | null;
            /**
             * Format: int64
             * @description 上次触发，unix 秒。从未触发或来源不记录（launchd）为 `None`。
             */
            last_ts?: number | null;
            /**
             * @description unit 名。systemd 为 `*.timer`；launchd 为补了 `.service` 后缀的 label。
             *     两者都可直接作为 `/services/{unit}` 的路径参数查详情。
             * @example logrotate.timer
             */
            name: string;
            /**
             * Format: int64
             * @description 下次触发，unix 秒。算不出为 `None`（timer 已停、launchd `StartInterval` 型）。
             */
            next_ts?: number | null;
            /**
             * @description 调度规则，人可读的原样表达（`OnCalendar=…`、`StartInterval=300s` 等）。
             *     一条任务可以有多条规则；降级路径拿不到时为空数组（「未知」而非「无规则」）。
             * @example [
             *       "OnCalendar=*-*-* 00:00:00"
             *     ]
             */
            schedule: string[];
            /** @description 作用域。 */
            scope: components["schemas"]["UnitScope"];
            /** @description 来源。 */
            source: components["schemas"]["TimerSource"];
            /**
             * @description 触发目标：systemd 是被拉起的 unit 名，launchd 是 program 路径。
             * @example logrotate.service
             */
            target?: string | null;
        };
        /**
         * @description 定时任务来源。
         *
         *     三种定时机制的底层完全不同（systemd bus / launchd plist / crontab 文件），
         *     前端统一成一张表，靠本枚举解释字段缺失：launchd 没有「上次触发」，
         *     `StartInterval` 型任务算不出「下次触发」。
         *
         *     cron 是规划中的第三来源（解析 crontab 文件族），实现后再加变体。
         * @enum {string}
         */
        TimerSource: "systemd_timer" | "launchd";
        /**
         * @description unit 操作。
         *
         *     前六个是运行时操作，`enable`/`disable`/`mask`/`unmask` 会改写磁盘上的符号链接。
         *     全部需要 polkit 授权；未提权且 polkit 判定 auth_admin 时返回
         *     [`crate::ErrorCode::ElevationRequired`]。
         * @enum {string}
         */
        UnitAction: "start" | "stop" | "restart" | "reload" | "enable" | "disable" | "mask" | "unmask";
        /** @description `POST /api/v1/services/{unit}/action` 的请求体。 */
        UnitActionReq: {
            /** @description 要执行的操作。 */
            action: components["schemas"]["UnitAction"];
        };
        /**
         * @description `POST /api/v1/services/{unit}/action` 的响应体。
         *
         *     systemd 的启停是**异步**的：接口返回只代表 job 已入队，不代表服务已经起来。
         *     前端应订阅 WS `services.changed` 频道等待终态，而不是立刻重新拉详情。
         */
        UnitActionResp: {
            /** @description 实际执行的操作。 */
            action: components["schemas"]["UnitAction"];
            active_state?: null | components["schemas"]["UnitActiveState"];
            /**
             * @description systemd 返回的 job 对象路径。降级到 `systemctl`（同步执行）时为 `None`。
             * @example /org/freedesktop/systemd1/job/4213
             */
            job?: string | null;
            /** @description 被操作的 unit 名。 */
            unit: string;
        };
        /**
         * @description unit 的活动状态（systemd `ActiveState`）。
         * @enum {string}
         */
        UnitActiveState: "active" | "reloading" | "inactive" | "failed" | "activating" | "deactivating" | "unknown";
        /**
         * @description `GET /api/v1/services/{unit}/deps` 的响应体：unit 依赖关系。
         *
         *     字段与 systemd `org.freedesktop.systemd1.Unit` 的同名属性一一对应。
         *     types crate 里没有这一项，暂由本文件定义（core 侧是同形的
         *     [`strixmaid_core::providers::service::UnitDeps`]）。
         */
        UnitDeps: {
            /** @description 顺序：本 unit 在它们之后启动。 */
            after: string[];
            /** @description 顺序：本 unit 在它们之前启动。 */
            before: string[];
            /** @description 对方停止时本 unit 也停止。 */
            binds_to: string[];
            /** @description 反向：哪些 unit `BindsTo` 本 unit。 */
            bound_by: string[];
            /** @description 反向互斥。 */
            conflicted_by: string[];
            /** @description 互斥。 */
            conflicts: string[];
            /** @description 对方停止 / 重启时本 unit 跟随。 */
            part_of: string[];
            /** @description 反向：哪些 unit `Requires` 本 unit。 */
            required_by: string[];
            /** @description 强依赖：本 unit 启动时它们必须成功启动。 */
            requires: string[];
            /** @description 类似 `requires`，但要求对方已经 active。 */
            requisite: string[];
            /** @description 触发本 unit 的 unit。 */
            triggered_by: string[];
            /** @description 本 unit 触发的 unit（socket / timer / path）。 */
            triggers: string[];
            /**
             * @description unit 名。
             * @example nginx.service
             */
            unit: string;
            /** @description 反向：哪些 unit `Wants` 本 unit。 */
            wanted_by: string[];
            /** @description 弱依赖。 */
            wants: string[];
        };
        /** @description `GET /api/v1/services/{unit}` 的响应体。 */
        UnitDetail: components["schemas"]["UnitSummary"] & {
            /**
             * Format: int64
             * @description 进入当前 active 状态的时刻（`ActiveEnterTimestamp`）。从未启动过为 `None`。
             *     前端的「已运行 N 天」由 `now - active_enter_ts` 算出。
             */
            active_enter_ts?: number | null;
            cgroup?: null | components["schemas"]["CgroupUsage"];
            /** @description `Documentation` 属性，man 页 / URL 列表，供前端做外链。 */
            documentation?: string[];
            /** @description drop-in 覆盖文件路径列表（`DropInPaths`），按生效顺序。空数组表示没有覆盖。 */
            drop_in_paths?: string[];
            /**
             * Format: int32
             * @description 主进程退出码（`ExecMainStatus`）。仅在已退出且以正常方式结束时有意义。
             */
            exit_code?: number | null;
            /**
             * @description 主 unit 文件路径（`FragmentPath`）。transient unit 为 `None`。
             * @example /usr/lib/systemd/system/nginx.service
             */
            fragment_path?: string | null;
            /**
             * Format: int32
             * @description 主进程 pid（`MainPID`）。未运行时为 `None`（systemd 报 0，这里归一成 `None`）。
             * @example 1234
             */
            main_pid?: number | null;
            /**
             * Format: int32
             * @description 累计重启次数（`NRestarts`）。频繁重启是排障的重要信号。
             * @example 0
             */
            n_restarts?: number | null;
            /**
             * @description 上次运行结果（`Result`）：`success` / `exit-code` / `signal` / `timeout` / `oom-kill` …
             *     原样透传，不枚举。
             * @example success
             */
            result?: string | null;
            /**
             * Format: int64
             * @description 最近一次状态变更时刻（`StateChangeTimestamp`）。
             */
            state_change_ts?: number | null;
            /** @description 运行身份（`User=`）。未设置即 root，为 `None`。 */
            user?: string | null;
        };
        /**
         * @description unit 文件的启用状态（`systemctl is-enabled` / systemd `UnitFileState`）。
         *
         *     注意它是**三值以上**的：`static` 与 `indirect` 既不是 enabled 也不是 disabled，
         *     前端不要简化成一个 checkbox。
         * @enum {string}
         */
        UnitEnableState: "enabled" | "enabled_runtime" | "linked" | "linked_runtime" | "alias" | "masked" | "masked_runtime" | "static" | "indirect" | "disabled" | "generated" | "transient" | "unknown";
        /** @description `GET /api/v1/services/{unit}/file` 的响应体：unit 文件原文。 */
        UnitFile: {
            /** @description drop-in 覆盖文件，按生效顺序。 */
            drop_ins?: components["schemas"]["UnitFileFragment"][];
            fragment?: null | components["schemas"]["UnitFileFragment"];
            /** @description unit 名。 */
            unit: string;
        };
        /** @description 一个 unit 文件片段。 */
        UnitFileFragment: {
            /** @description 文件原文（UTF-8）。**不做任何解析或改写**，前端按 ini 高亮即可。 */
            content: string;
            /**
             * @description 绝对路径。
             * @example /usr/lib/systemd/system/nginx.service
             */
            path: string;
        };
        /**
         * @description unit 的加载状态（systemd `LoadState`）。
         * @enum {string}
         */
        UnitLoadState: "loaded" | "not_found" | "bad_setting" | "error" | "masked" | "unknown";
        /**
         * @description unit 作用域。
         * @enum {string}
         */
        UnitScope: "system" | "user";
        /** @description `GET /api/v1/services` 列表项。 */
        UnitSummary: {
            /** @description 活动状态。 */
            active_state: components["schemas"]["UnitActiveState"];
            /**
             * @description `Description` 属性；未设置时 systemd 会回落成 unit 名本身。
             * @example A high performance web server and a reverse proxy server
             */
            description: string;
            enable_state?: null | components["schemas"]["UnitEnableState"];
            /** @description 加载状态。 */
            load_state: components["schemas"]["UnitLoadState"];
            /**
             * @description 完整 unit 名，**含后缀**。这是路径参数 `{unit}` 的取值，需 URL 编码（`@` 实例名里有特殊字符）。
             * @example nginx.service
             */
            name: string;
            /** @description 所属作用域。 */
            scope: components["schemas"]["UnitScope"];
            /**
             * @description 子状态，取值由 unit 类型决定（service 有 `running`/`exited`/`dead`/`failed`…）。
             *     **不要**枚举它——systemd 各版本取值不同，原样展示即可。
             * @example running
             */
            sub_state: string;
            /**
             * @description unit 类型，即 `name` 的后缀：`service` / `socket` / `timer` / `target` / `mount` / `path` …
             * @example service
             */
            unit_type: string;
        };
        /** @description 当前会话的用户级能力。 */
        UserCapabilities: {
            /**
             * @description 能否走 `/api/v1/auth/elevate/*` 提权（通常等价于「在 sudo / wheel 组」或 polkit 允许 auth_admin）。
             *     为 `false` 时**不要**显示提权入口，否则用户点了必然失败。
             * @example true
             */
            can_elevate: boolean;
            /**
             * @description 能否管理 unit（polkit 判定 `org.freedesktop.systemd1.manage-units` 为 yes 或 auth_admin）。
             *     为 `false` 时服务页的操作按钮禁用，但列表仍可看。
             * @example false
             */
            can_manage_units: boolean;
            /**
             * @description 能否读取**全量**日志（在 `systemd-journal` / `adm` / `wheel` 组，或有 journald ACL）。
             *     为 `false` 时日志页仍可用，但只能看到自己的条目——必须在 UI 上明说，否则用户会以为日志丢了。
             * @example false
             */
            can_read_journal: boolean;
            /**
             * @description 当前是否已提权。提权成功后此位翻为 `true`，`can_manage_units` 等通常随之变 `true`。
             * @example false
             */
            elevated: boolean;
            /** @description 所属组名列表（含主组）。前端可据此生成「你不在 xxx 组」这类具体说明。 */
            groups?: string[];
            /**
             * @description 系统用户名。
             * @example alice
             */
            name: string;
            /**
             * Format: int32
             * @description 系统 uid。
             * @example 1000
             */
            uid: number;
        };
        /**
         * @description 平台支持的日志清理方式。
         *
         *     journald 支持按保留期 / 目标大小收缩（`--vacuum-time` / `--vacuum-size`）；
         *     macOS 统一日志没有等价物，只有 `log erase` 的「全部抹掉」一档。
         *     前端按 [`LogUsage::modes`] 决定清理对话框长什么样，而不是按平台猜。
         * @enum {string}
         */
        VacuumMode: "keep_duration" | "max_size" | "erase_all";
        /** @description `POST /api/v1/logs/vacuum` 的请求体。**三个字段必须恰好给一个**。 */
        VacuumReq: {
            /** @description 全部抹除（macOS `log erase --all`）。必须显式传 `true`。 */
            erase_all?: boolean | null;
            /**
             * Format: int64
             * @description 只保留最近 N 秒的日志（`journalctl --vacuum-time`）。
             *     注意 journald 只清**已归档**的日志文件，当前活跃文件不动——
             *     清理后占用不会精确等于期望值。
             * @example 604800
             */
            keep_secs?: number | null;
            /**
             * Format: int64
             * @description 收缩到 N 字节以内（`journalctl --vacuum-size`）。同样只清已归档文件。
             */
            max_bytes?: number | null;
        };
        /** @description `POST /api/v1/logs/vacuum` 的响应体。 */
        VacuumResp: {
            /**
             * Format: int64
             * @description 清理后占用。
             */
            after_bytes?: number | null;
            /**
             * Format: int64
             * @description 清理前占用。测不到为 `None`。
             */
            before_bytes?: number | null;
            /** @description 工具的原话摘要（如 journalctl 的 `Vacuuming done, freed …`）。 */
            detail?: string | null;
        };
    };
    responses: never;
    parameters: never;
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    list_audit: {
        parameters: {
            query?: {
                /** @description 起始时刻（**含**），unix 秒。 */
                since?: number;
                /**
                 * @description 结束时刻（**不含**），unix 秒。
                 *
                 *     区间是左闭右开 `[since, until)`，与指标查询（`design.md` §7.2）一致。
                 *     闭区间会把恰好落在 `until` 那一秒的记录带进来，按整点分页时相邻两页会重叠。
                 */
                until?: number;
                /**
                 * @description 按发起者用户名精确过滤。
                 * @example alice
                 */
                username?: string;
                /**
                 * @description 按 `action` 前缀过滤，如 `service.` 命中所有服务操作。
                 * @example service.
                 */
                action?: string;
                /** @description 按结果过滤。查 `denied` 是排查权限问题的主要入口。 */
                result?: components["schemas"]["AuditResult"];
                /** @description 翻页游标：只返回 `id` 严格小于该值的记录（由新到旧）。缺省从最新一条开始。 */
                before_id?: number;
                /**
                 * @description 每页条数。缺省 100，上限由服务端限制。
                 * @example 100
                 */
                limit?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description 一页审计记录，按 id 降序 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuditPage"];
                };
            };
            /** @description 参数不合法（limit 为 0 或超过 1000、since > until） */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 未认证 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 会话未提权（`elevation_required`） */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 读取 audit_log 失败 */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    elevate_respond: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AuthRespondReq"];
            };
        };
        responses: {
            /** @description 提权完成或需要更多回应 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthOutcome"];
                };
            };
            /** @description 未登录或认证失败 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 无法创建 admin worker（helper 不是 root） */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 认证会话不存在或已超时 */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    elevate_start: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AuthStartReq"];
            };
        };
        responses: {
            /** @description 第一轮提示 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthStartResp"];
                };
            };
            /** @description 未登录 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description PAM helper 不可用 */
            501: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    logout: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description 已登出 */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description 未登录 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    respond: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AuthRespondReq"];
            };
        };
        responses: {
            /** @description 认证完成或需要更多回应 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthOutcome"];
                };
            };
            /** @description 认证失败 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 认证会话不存在或已超时 */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    session: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description 会话信息 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SessionInfo"];
                };
            };
            /** @description 未登录或已过期 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    start: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AuthStartReq"];
            };
        };
        responses: {
            /** @description 第一轮提示 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthStartResp"];
                };
            };
            /** @description 用户名为空 */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description PAM 在第一轮就拒绝了（如用户不存在） */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description PAM helper 不可用，无法登录 */
            501: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    capabilities: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description 两层能力 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Capabilities"];
                };
            };
        };
    };
    list_dir: {
        parameters: {
            query: {
                /**
                 * @description 绝对路径。必须以 `/` 开头且**不含 `..`**——服务端做规范化后校验，
                 *     不合法返回 [`crate::ErrorCode::InvalidRequest`]。
                 * @example /etc
                 */
                path: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description 目录列表 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DirListing"];
                };
            };
            /** @description 路径不合法（相对路径、不是目录） */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 未认证，或会话的 worker 已退出 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 无权限读取，或路径在 files.allowed_roots 之外 */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 路径不存在 */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    read_file: {
        parameters: {
            query: {
                /**
                 * @description 绝对路径。必须以 `/` 开头且**不含 `..`**——服务端做规范化后校验，
                 *     不合法返回 [`crate::ErrorCode::InvalidRequest`]。
                 * @example /etc
                 */
                path: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description 文件内容 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["FileContent"];
                };
            };
            /** @description 路径不合法、二进制文件或超出大小上限 */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 未认证，或会话的 worker 已退出 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 无权限读取，或路径在 files.allowed_roots 之外 */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 文件不存在 */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    health: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description 服务存活 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HealthResponse"];
                };
            };
        };
    };
    query_logs: {
        parameters: {
            query?: {
                /**
                 * @description **最低**优先级：只返回严重程度 >= 该值的条目（即数字 <= `priority.as_u8()`）。
                 *     传 `warning` 会同时返回 `emerg`..`warning`。缺省不过滤。
                 * @example warning
                 */
                priority?: components["schemas"]["LogPriority"];
                /**
                 * @description 起始时刻（含），unix 秒。缺省不设下界。
                 * @example 1756252800
                 */
                since?: number;
                /** @description 结束时刻（含），unix 秒。缺省不设上界。 */
                until?: number;
                /**
                 * @description 按 unit 过滤，需完整 unit 名（对应 `journalctl -u`）。
                 * @example nginx.service
                 */
                unit?: string;
                /**
                 * @description 按 boot 过滤。取值为 [`BootInfo::boot_id`]（32 位 hex），或相对偏移
                 *     （`"0"` = 本次启动，`"-1"` = 上一次）。缺省不限制 boot。
                 * @example 0
                 */
                boot?: string;
                /**
                 * @description 全文关键字，对 `MESSAGE` 做匹配（对应 `journalctl -g`，大小写不敏感）。
                 *     旧版 journalctl 无 `-g` 时由服务端在读出后自行过滤。
                 * @example connection refused
                 */
                q?: string;
                /**
                 * @description 翻页游标：从**上一页返回的游标**继续，方向为**由新到旧**（journald 默认的 tail 方向）。
                 *     缺省从最新一条开始。游标本身对应的条目**不重复返回**。
                 */
                cursor?: string;
                /**
                 * @description 每页条数。缺省 200，上限由服务端限制（建议 1000）；越界返回
                 *     [`crate::ErrorCode::InvalidRequest`]。
                 * @example 200
                 */
                limit?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description 一页日志（范围按登录用户或已提权的管理身份裁剪） */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["LogPage"];
                };
            };
            /** @description 参数不合法（limit 越界、since > until、boot / cursor 格式错） */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 未认证，或会话的 worker 已退出 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 读不到日志且会话未提权；带 `can_retry_elevated` */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 本机没有 journalctl */
            501: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description journalctl 超时 */
            504: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    list_boots: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description boot 列表 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BootInfo"][];
                };
            };
            /** @description 未认证，或会话的 worker 已退出 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 本机没有 journalctl */
            501: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    log_entry: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description journald 游标（`__CURSOR`），需 URL 编码 */
                cursor: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description 全字段详情 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["LogEntryDetail"];
                };
            };
            /** @description 游标格式不合法 */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 未认证，或会话的 worker 已退出 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 游标对应的条目不存在，或对登录用户不可见 */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 本机没有 journalctl */
            501: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    log_usage: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description 占用与清理能力 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["LogUsage"];
                };
            };
            /** @description 未认证，或会话的 worker 已退出 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 本机没有可用的日志后端 */
            501: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    log_vacuum: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["VacuumReq"];
            };
        };
        responses: {
            /** @description 清理完成，带清理前后占用与工具摘要 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["VacuumResp"];
                };
            };
            /** @description 三个字段没有恰好给一个 */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 未认证，或会话的 worker 已退出 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 被拒且未提权；带 `can_retry_elevated` */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 本机日志后端不支持所选清理方式 */
            501: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    current: {
        parameters: {
            query?: {
                /**
                 * @description 节点 id，缺省 `local`。非 local 时返回该节点最近一帧 `agent.snapshot`；
                 *     节点未连接过或无快照时 404。
                 * @example local
                 */
                node?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description 最新一轮快照 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MetricSnapshot"];
                };
            };
            /** @description 未认证 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 节点未连接过或尚无快照 */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    query: {
        parameters: {
            query: {
                /**
                 * @description 要查的序列，**逗号分隔**。每项为 `series.id`（数字）或 `metric{labels}` 形式，
                 *     如 `cpu.usage` / `disk.read_bytes{dev=sda}`。
                 *
                 *     用逗号而非重复参数，是为了让 axum 的 `Query<T>` 无需额外依赖即可解析。
                 * @example cpu.usage,mem.available
                 */
                series: string;
                /**
                 * @description 起始时刻（含），unix 秒。
                 * @example 1756249200
                 */
                from: number;
                /**
                 * @description 结束时刻（含），unix 秒。必须 >= `from`，否则返回
                 *     [`crate::ErrorCode::InvalidRequest`]。
                 * @example 1756252800
                 */
                to: number;
                /**
                 * @description 期望的点间隔，秒。服务端据此**自动选层**：挑选桶宽 <= `step` 的最粗一层，
                 *     保证返回点数可控。缺省时按 `(to - from) / 目标点数` 推算。
                 * @example 60
                 */
                step?: number;
                /**
                 * @description 节点 id，缺省 `local`。非 local 节点没有内存环，`live` 层不可用，
                 *     一律走落盘层（roadmap/05 §3.3）。
                 * @example local
                 */
                node?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description 查询结果 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MetricQueryResp"];
                };
            };
            /** @description 参数不合法：to < from、series 为空或格式错误 */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 未认证 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 数据库查询失败 */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    series: {
        parameters: {
            query?: {
                /**
                 * @description 按节点过滤。缺省返回全部节点（MVP 只有 `local`）。
                 * @example local
                 */
                node?: string;
                /**
                 * @description 按指标名前缀过滤，如 `disk.` 会命中 `disk.read_bytes` 等。
                 * @example disk.
                 */
                prefix?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description 序列列表，按 metric、labels 排序 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SeriesMeta"][];
                };
            };
            /** @description 未认证 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 读取 series 表失败 */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    list_nodes: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description 节点列表 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["NodeInfo"][];
                };
            };
            /** @description 未认证 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    create_node: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateNodeReq"];
            };
        };
        responses: {
            /** @description 已登记，token 只出现这一次 */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CreateNodeResp"];
                };
            };
            /** @description id 不合法或 name 为空 */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 未认证 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 会话未提权（`elevation_required`） */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description id 已存在 */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    delete_node: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 节点 id */
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description 已删除 */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description 不能删除 local */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 未认证 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 会话未提权（`elevation_required`） */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 节点不存在 */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    list: {
        parameters: {
            query?: {
                /** @description 排序字段，缺省 [`ProcessSortKey::Cpu`]。 */
                sort?: components["schemas"]["ProcessSortKey"];
                /** @description 排序方向，缺省 [`SortOrder::Desc`]。 */
                order?: components["schemas"]["SortOrder"];
                /**
                 * @description 只看某个用户的进程。可传用户名或数字 uid。
                 * @example www-data
                 */
                user?: string;
                /**
                 * @description 关键字，对 `name` 与 `cmdline` 做大小写不敏感的子串匹配。
                 * @example nginx
                 */
                q?: string;
                /**
                 * @description 树视图。**只影响筛选与排序语义，不改变响应形状**——响应始终是平铺的
                 *     [`ProcessSummary`] 数组，树由前端按 `ppid` 拼。
                 *
                 *     为 `true` 时服务端保证：凡命中 `q` / `user` 的进程，其**全部祖先**也一并返回
                 *     （否则树会断链），并按父子关系做深度优先排序。
                 */
                tree?: boolean;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description 进程列表 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ProcessSummary"][];
                };
            };
            /** @description 未认证，或会话的 worker 已退出 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 采集任务异常 */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    detail: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 进程 id */
                pid: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description 进程详情 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ProcessDetail"];
                };
            };
            /** @description pid 不合法 */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 未认证，或会话的 worker 已退出 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 进程不存在 */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    renice: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 进程 id */
                pid: number;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ReniceReq"];
            };
        };
        responses: {
            /** @description 已调整 */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description pid 或 nice 值不合法 */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 未认证，或会话的 worker 已退出 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 内核拒绝（不是属主，或调低优先级需要 root）且会话未提权，`can_retry_elevated = true`；启用管理访问后重试可成功 */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 进程不存在 */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    signal: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 进程 id */
                pid: number;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SignalReq"];
            };
        };
        responses: {
            /** @description 已发送 */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description pid 不合法或为 1 */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 未认证，或会话的 worker 已退出 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 内核拒绝（不是属主）且会话未提权，`can_retry_elevated = true`；启用管理访问后重试可成功 */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 进程不存在 */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    list_units: {
        parameters: {
            query?: {
                /**
                 * @description 按 unit 类型过滤，取值即后缀名（`service` / `timer` / `socket` …）。
                 *     缺省返回所有类型。
                 * @example service
                 */
                type?: string;
                /** @description 按活动状态过滤。缺省不过滤。 */
                state?: components["schemas"]["UnitActiveState"];
                /**
                 * @description 按是否开机自启过滤。`true` 匹配 `enabled` / `enabled_runtime`，
                 *     `false` 匹配 `disabled`；`static` / `indirect` 等既不匹配 `true` 也不匹配 `false`。
                 */
                enabled?: boolean;
                /**
                 * @description 关键字，对 unit 名与 `description` 做大小写不敏感的子串匹配。
                 * @example nginx
                 */
                q?: string;
                /** @description 作用域，缺省为 [`UnitScope::System`]。 */
                scope?: components["schemas"]["UnitScope"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description unit 列表 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["UnitSummary"][];
                };
            };
            /** @description 未认证，或会话的 worker 已退出 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 本机没有 systemd */
            501: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description systemd 暂时不可达（bus 断开 / user manager 未启动） */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description systemd 无响应 */
            504: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    list_timers: {
        parameters: {
            query?: {
                /** @description 作用域，缺省 `system`。`user` 指登录用户自己的 user manager。 */
                scope?: components["schemas"]["UnitScope"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description 定时任务列表 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["TimerEntry"][];
                };
            };
            /** @description 未认证，或会话的 worker 已退出 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 本机没有服务管理器 */
            501: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 服务管理器暂时不可达 */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    unit_detail: {
        parameters: {
            query?: {
                /** @description 作用域，缺省 `system`。`user` 指登录用户自己的 user manager。 */
                scope?: components["schemas"]["UnitScope"];
            };
            header?: never;
            path: {
                /**
                 * @description 完整 unit 名，含后缀，需 URL 编码
                 * @example nginx.service
                 */
                unit: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description unit 详情 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["UnitDetail"];
                };
            };
            /** @description unit 名不合法 */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 未认证，或会话的 worker 已退出 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description unit 不存在 */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 本机没有 systemd */
            501: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description systemd 暂时不可达 */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    unit_action: {
        parameters: {
            query?: {
                /** @description 作用域，缺省 `system`。`user` 指登录用户自己的 user manager。 */
                scope?: components["schemas"]["UnitScope"];
            };
            header?: never;
            path: {
                /**
                 * @description 完整 unit 名，含后缀，需 URL 编码
                 * @example nginx.service
                 */
                unit: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UnitActionReq"];
            };
        };
        responses: {
            /** @description job 已入队（bus）或已执行完（systemctl） */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["UnitActionResp"];
                };
            };
            /** @description 未认证，或会话的 worker 已退出 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 被拒。通常是 `permission_denied`——polkit 以登录用户的身份拒绝了本次操作；未提权时带 `can_retry_elevated=true`，提示前端「启用管理访问后重试」。若升级重试的瞬间 admin worker 恰好已因提权超时被回收，则为 `elevation_required` */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description unit 不存在 */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description systemd 拒绝（已 mask、无 ExecReload 等） */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 本机没有 systemd */
            501: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    unit_deps: {
        parameters: {
            query?: {
                /** @description 作用域，缺省 `system`。`user` 指登录用户自己的 user manager。 */
                scope?: components["schemas"]["UnitScope"];
            };
            header?: never;
            path: {
                /**
                 * @description 完整 unit 名，含后缀，需 URL 编码
                 * @example nginx.service
                 */
                unit: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description 依赖关系 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["UnitDeps"];
                };
            };
            /** @description 未认证，或会话的 worker 已退出 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description unit 不存在 */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 本机没有 systemd */
            501: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    unit_file: {
        parameters: {
            query?: {
                /** @description 作用域，缺省 `system`。`user` 指登录用户自己的 user manager。 */
                scope?: components["schemas"]["UnitScope"];
            };
            header?: never;
            path: {
                /**
                 * @description 完整 unit 名，含后缀，需 URL 编码
                 * @example nginx.service
                 */
                unit: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description unit 文件 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["UnitFile"];
                };
            };
            /** @description 未认证，或会话的 worker 已退出 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 文件对登录用户不可读 */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description unit 不存在 */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 本机没有 systemd */
            501: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    system_health: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description 健康报告；`items` 为空表示一切正常 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HealthReport"];
                };
            };
            /** @description 未认证，或会话的 worker 已退出 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 采集任务异常 */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    set_hostname: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SetHostnameReq"];
            };
        };
        responses: {
            /** @description 已修改 */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description 主机名不合法 */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 未认证，或会话的 worker 已退出 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 需要管理访问：会话尚未提权，`code = elevation_required`、`can_retry_elevated = true`，提权后重试即可 */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 写入失败 */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    info: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description 完整主机信息 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SystemInfo"];
                };
            };
            /** @description 未认证，或会话的 worker 已退出 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 采集任务异常 */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    power: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["PowerReq"];
            };
        };
        responses: {
            /** @description 已受理 */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description 未认证，或会话的 worker 已退出 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 需要管理访问：会话尚未提权，`code = elevation_required`、`can_retry_elevated = true`，提权后重试即可 */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 执行失败 */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 没有 systemctl */
            501: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    time: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description 时间信息 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["TimeInfo"];
                };
            };
            /** @description 未认证，或会话的 worker 已退出 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 采集任务异常 */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    set_timezone: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SetTimezoneReq"];
            };
        };
        responses: {
            /** @description 已修改 */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description 时区名不合法或不存在 */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 未认证，或会话的 worker 已退出 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 需要管理访问：会话尚未提权，`code = elevation_required`、`can_retry_elevated = true`，提权后重试即可 */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 写入失败 */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    list_terminals: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description 本会话的终端 */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["TerminalInfo"][];
                };
            };
            /** @description 未认证 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    create_terminal: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateTerminalReq"];
            };
        };
        responses: {
            /** @description 已创建 */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CreateTerminalResp"];
                };
            };
            /** @description shell 不合法（不存在、或不在 /etc/shells 里） */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 未认证，或会话的 worker 已退出 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 指定了其他用户但会话未提权 */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 本会话的终端数已达上限 */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    delete_terminal: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 终端 id */
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description 已关闭 */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description 未认证 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 终端不存在或已关闭 */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
    resize_terminal: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 终端 id */
                id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ResizeReq"];
            };
        };
        responses: {
            /** @description 已生效 */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description 行列数必须大于 0 */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 未认证 */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
            /** @description 终端不存在或已关闭 */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiError"];
                };
            };
        };
    };
}
